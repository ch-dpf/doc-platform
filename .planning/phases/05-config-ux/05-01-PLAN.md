---
phase: 05-config-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/knowbase-ui/src/utils/libraryConfig.js
  - frontend/knowbase-ui/src/utils/libraryConfig.test.js
  - frontend/knowbase-ui/package.json
  - frontend/knowbase-ui/src/components/CreateLibraryWizard.vue
  - frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue
  - frontend/knowbase-ui/src/utils/fieldImpactHints.js
autonomous: true
requirements:
  - CFG-01
  - CFG-02
must_haves:
  truths:
    - "diffLibraryConfig detects changes to parsing.tableExtraction, parsing.ocrEnabled, and all chunking.* root fields"
    - "diffLibraryConfig detects cleaning.removeHeaderFooter and semanticSimilarityThreshold if present in config"
    - "EditLibrarySettingsDrawer no longer shows «配置未变更» when only nested parsing/chunking/cleaning fields changed"
    - "Wizard and edit drawer show brief impact hints for OCR, tableExtraction, chunkingStrategy"
  artifacts:
    - path: "frontend/knowbase-ui/src/utils/libraryConfig.test.js"
      provides: "Automated diff regression tests for nested fields"
    - path: "frontend/knowbase-ui/src/utils/fieldImpactHints.js"
      provides: "IMPACT_HINTS map for parsing/chunking fields"
  key_links:
    - from: "EditLibrarySettingsDrawer.vue"
      to: "libraryConfig.js"
      via: "diffLibraryConfig(beforeCfg, nextConfig)"
      pattern: "diffLibraryConfig"
    - from: "fieldImpactHints.js"
      to: "CreateLibraryWizard.vue"
      via: "el-tooltip or hint paragraph"
      pattern: "fieldImpactHint|IMPACT_HINTS"
---

<objective>
Make library config diff/save reliable for nested fields (CFG-01) and add operator-readable impact hints for key parsing options (CFG-02).

Purpose: ROADMAP Phase 5 success criterion — tableExtraction changes save without «配置未变更» false negative; ops understand OCR/table/chunk choices.
Output: libraryConfig.test.js, diff fixes, fieldImpactHints wired in wizard + edit drawer.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/docs/INGEST-PIPELINE.md
@.planning/docs/FILE-TYPE-PROCESSING.md
@frontend/knowbase-ui/src/utils/libraryConfig.js
@frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: diffLibraryConfig tests + gap fixes (CFG-01)</name>
  <files>frontend/knowbase-ui/src/utils/libraryConfig.js, frontend/knowbase-ui/src/utils/libraryConfig.test.js, frontend/knowbase-ui/package.json</files>
  <read_first>
    - frontend/knowbase-ui/src/utils/libraryConfig.js — diffLibraryConfig, REINDEX_FIELDS, CONFIG_FIELD_SPECS
    - frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue — submit() diff gate (~604–611), buildSubmitConfig(), mergeLoadedConfig()
    - .planning/docs/FILE-TYPE-PROCESSING.md — appendix C field paths
  </read_first>
  <behavior>
    - tableExtraction change text-only → structured produces one change with field parsing.tableExtraction and needsReindex true
    - ocrEnabled toggle detected
    - chunkingStrategy, chunkSize, minParagraphLength changes detected at root
    - cleaning.removeHeaderFooter toggle detected
    - semanticSimilarityThreshold change detected (add to diff if missing)
    - Identical before/after (deep equal on compared fields) returns empty array
    - Shallow parsing object replacement with same leaf values returns empty (no false positive)
  </behavior>
  <action>
    Add vitest as devDependency in frontend/knowbase-ui/package.json; add script "test": "vitest run".

    Create libraryConfig.test.js importing diffLibraryConfig, diffNeedsReindex from libraryConfig.js.

    Write tests FIRST for nested parsing/chunking/cleaning cases above (RED if gaps exist).

    Fix libraryConfig.js gaps discovered by tests:
    - Add semanticSimilarityThreshold to CONFIG_FIELD_SPECS + REINDEX_FIELDS if absent
    - Ensure all cleaning.* keys in EditLibrarySettingsDrawer form are compared (removeHeaderFooter, removeWatermark, maskPhone, maskIdCard, stopwordFilter already looped — verify needsReindex flags match REINDEX_FIELDS)
    - If false «配置未变更» root cause is snapshot/compare mismatch: ensure buildSubmitConfig() and clonePayload() produce structurally equivalent parsing/cleaning objects (deep merge parsing nested keys; do not strip unchanged nested defaults)

    Do NOT change lockPipeline backend behavior — scope is diff detection + save gate only.
  </action>
  <acceptance_criteria>
    - npm test passes all libraryConfig.test.js cases
    - tableExtraction-only change yields changes.length === 1
    - No missing nested field listed in FILE-TYPE-PROCESSING appendix C that appears in EditLibrarySettingsDrawer
  </acceptance_criteria>
  <verify>
    <automated>cd frontend/knowbase-ui && npm install && npm test</automated>
  </verify>
  <done>CFG-01: nested diff covered by automated tests; known false-negative paths fixed.</done>
</task>

<task type="auto">
  <name>Task 2: Field impact hints in wizard and edit drawer (CFG-02)</name>
  <files>frontend/knowbase-ui/src/utils/fieldImpactHints.js, frontend/knowbase-ui/src/components/CreateLibraryWizard.vue, frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue</files>
  <read_first>
    - frontend/knowbase-ui/src/components/CreateLibraryWizard.vue — OCR tooltip (~168–174), tableExtraction select (~179)
    - frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue — parsing form items (~151–190), chunking fields
    - .planning/docs/FILE-TYPE-PROCESSING.md — §2 matrix rows for PDF/Word/Excel tableExtraction and OCR
  </read_first>
  <action>
    Create fieldImpactHints.js exporting IMPACT_HINTS map keyed by dot-path:
    - parsing.ocrEnabled — 扫描件 PDF 图片页需开启；依赖服务端 tessdata
    - parsing.tableExtraction — Word/PDF 复杂表格用 structured；Excel 周报保持 text-only（structured 对 xlsx 无效）
    - parsing.imageExtraction — 嵌入图片 OCR 描述或跳过
    - chunkingStrategy — paragraph-first 适合 Excel 周报；semantic 不适合表格续行
    - chunkSize / minParagraphLength — 影响块边界与表头过滤结果；变更需重索引

    Add helper fieldImpactHint(path) returning string or empty.

    CreateLibraryWizard: add el-tooltip + short hint paragraph under tableExtraction, chunkingStrategy (reuse OCR tooltip pattern). Use IMPACT_HINTS — not duplicate prose inline.

    EditLibrarySettingsDrawer: mirror same hints on parsing tab and chunking section; use class settings-form__tip under each el-form-item.

    Keep hints ≤ 1 sentence each; Chinese operator tone matching existing wizard copy.
  </action>
  <acceptance_criteria>
    - tableExtraction and OCR fields show impact hint in both wizard and edit drawer
    - chunkingStrategy shows strategy-specific hint
    - Hints sourced from fieldImpactHints.js (single source)
  </acceptance_criteria>
  <verify>
    <automated>rg -c "fieldImpactHint|IMPACT_HINTS" frontend/knowbase-ui/src/components/CreateLibraryWizard.vue frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue frontend/knowbase-ui/src/utils/fieldImpactHints.js | rg -v "^#" | ForEach-Object { if ($_ -match ":0$") { exit 1 } }; exit 0</automated>
  </verify>
  <done>CFG-02: operators see why OCR/table/chunk settings matter before saving.</done>
</task>

<task type="auto">
  <name>Task 3: Save-flow verification for nested parsing changes</name>
  <files>frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue</files>
  <read_first>
    - frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue — submit(), confirmSave(), snapshotPayload
    - frontend/knowbase-ui/src/api/library.js — updateVectorLibrarySettings
  </read_first>
  <action>
    Audit submit() path: when user toggles parsing.tableExtraction only, diffLibraryConfig must return non-empty BEFORE ElMessage.info('配置未变更') gate.

    If test from Task 1 passes but UI still false-negatives: compare beforeCfg source — ensure snapshotPayload taken at load via clonePayload() uses same mergeLoadedConfig shape as buildSubmitConfig() output (parsing nested object must not be re-defaulted between snapshot and submit).

    Fix merge/snapshot asymmetry if found (e.g. normalize parsing sub-object keys in both clonePayload and buildSubmitConfig).

    Add inline comment above diff gate documenting CFG-01 contract.

    Run npm test + npm run build to confirm no regressions.
  </action>
  <acceptance_criteria>
    - tableExtraction-only edit triggers confirmSave dialog (not «配置未变更»)
    - npm run build succeeds
  </acceptance_criteria>
  <verify>
    <automated>cd frontend/knowbase-ui && npm test && npm run build</automated>
  </verify>
  <done>End-to-end save gate reliable for nested parsing fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form → PUT /api/v1/vector-libraries/{id} | Config payload from operator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Client-side diff only | accept | Backend VectorLibraryService still authoritative; diff is UX gate |
| T-05-SC | Tampering | vitest npm install | mitigate | vitest is [VERIFIED] mainstream; run npm audit; no [SLOP] packages |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | Add vitest@^3.x only; slopcheck before install |
</threat_model>

<verification>
- `cd frontend/knowbase-ui && npm test && npm run build` passes
- Manual: change tableExtraction in edit drawer → diff preview shows «表格提取» row → save succeeds
</verification>

<success_criteria>
- CFG-01 and CFG-02 requirements satisfied with automated diff tests and visible operator hints
- ROADMAP Phase 5 success criterion met: nested field save without false «配置未变更»
</success_criteria>

<output>
Create `.planning/phases/05-config-ux/05-01-SUMMARY.md` when done
</output>

---
phase: 01-ingest-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/docs/INGEST-PIPELINE.md
autonomous: true
requirements:
  - PIPE-02
  - PIPE-03
must_haves:
  truths:
    - "Document describes full single-document ingest path from upload through document_chunk write (PIPE-02)"
    - "Stage × class × API matrix links frontend entry to backend service for each pipeline stage (PIPE-03)"
    - "Preview vs index path divergence documented with overrideChunk and dual-service gap (D-14)"
    - "Ingest stages name DocumentPipelineService, IndexingService, IndexingChunkFilter explicitly (D-13)"
  artifacts:
    - path: ".planning/docs/INGEST-PIPELINE.md"
      provides: "§3 ingest flow, §4 stage/class/API matrix, §4.3 preview gap"
      contains: "IndexingChunkFilter"
  key_links:
    - from: ".planning/docs/INGEST-PIPELINE.md"
      to: "DocumentIngestor.java"
      via: "upload stage table row"
      pattern: "DocumentIngestor|ingestOne"
    - from: ".planning/docs/INGEST-PIPELINE.md"
      to: "IngestView.vue"
      via: "API matrix UI entry column"
      pattern: "IngestView|parse-preview|chunk-preview|upload"
    - from: ".planning/docs/INGEST-PIPELINE.md"
      to: "IndexingService.java"
      via: "chunk → filter → embed stage"
      pattern: "IndexingService|IndexingChunkFilter|document_chunk"
---

<objective>
Document the single-document ingest pipeline (PIPE-02) and the stage/class/API cross-reference matrix (PIPE-03), including preview-vs-index current gaps (D-14).

Purpose: Enables developers and ops to follow one document from IngestView upload to pgvector chunk persistence without ambiguity — the ROADMAP Phase 1 success criterion.
Output: INGEST-PIPELINE.md §3 and §4 complete; §3 placeholder from Plan 01 replaced.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingest-pipeline/01-CONTEXT.md
@.planning/phases/01-ingest-pipeline/01-RESEARCH.md
@.planning/phases/01-ingest-pipeline/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/docs/INGEST-PIPELINE.md
</context>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| `.planning/docs/INGEST-PIPELINE.md` §3–§4 | 02 | Ingest pipeline mermaid, stage table, API matrix, preview gap |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: PIPE-02 ingest pipeline flow and stage table</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - knowbase-service/src/main/java/com/knowbase/ingest/service/DocumentIngestor.java — ingestOne, storeAndProcess
    - knowbase-service/src/main/java/com/knowbase/ingest/service/DocumentPipelineService.java — scheduleProcessAfterCommit, processAsync
    - knowbase-service/src/main/java/com/knowbase/vector/service/IndexingService.java — index, chunk, filter, embed
    - knowbase-service/src/main/java/com/knowbase/vector/chunk/IndexingChunkFilter.java — removeHeaderOnlyChunks
    - .planning/phases/01-ingest-pipeline/01-RESEARCH.md — System Architecture mermaid, Pattern 2 post-commit async
    - .planning/phases/01-ingest-pipeline/01-PATTERNS.md — §3 stage table template
  </read_first>
  <action>
    Replace §3 placeholder with full PIPE-02 content per D-13 stage granularity:

    3.1 Mermaid sequenceDiagram: IngestView → DocumentController → DocumentIngestor → DocumentPipelineService (afterCommit async) → DocumentIndexCoordinator → IndexingService → document_chunk. Include object storage nodes for raw/ and parsed.txt.

    3.2 Numbered stage table with columns: # | 阶段 | 关键类 | 配置来源 (LibraryConfigResolver) | 持久化/输出
    Cover all stages: 上传校验 → 异步解析 → 文本规范化 → 内容清洗 → 索引触发(含 manual-review) → 分块 → IndexingChunkFilter表头过滤 → 嵌入写入 → ChunkMetadataBuilder元数据.
    For each stage cite the exact resolver method (allowedMimeTypes, parseOptionsFor, normalizationFor, cleaningFor, chunkingFor, embeddingFor, requiresManualReview, retrievalFor).

    3.3 documentMetadata flow (D-06): IngestView.resolveDocumentMetadataParam → upload query param → doc_metadata.custom_metadata_json → ChunkMetadataBuilder — explicitly state it does NOT alter parse/chunk params in v1.

    3.4 Note post-commit async pattern (TransactionSynchronization afterCommit) and why upload returns before parse/index completes.

    Do NOT write anti-pattern gallery or Appendix A detail here — cross-ref §8 and 附录 A.
  </action>
  <acceptance_criteria>
    - All nine pipeline stages from D-13 present with class names
    - IndexingChunkFilter named as distinct stage before embed
    - document_chunk persistence named as final output
    - Mermaid sequenceDiagram syntactically valid
    - documentMetadata semantic-only behavior stated
  </acceptance_criteria>
  <verify>
    <automated>rg -c "DocumentIngestor|DocumentPipelineService|IndexingService|IndexingChunkFilter|document_chunk|scheduleProcessAfterCommit" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Reader can follow upload → parse → clean → chunk → filter → embed → document_chunk without opening source files.</done>
</task>

<task type="auto">
  <name>Task 2: PIPE-03 stage/class/API matrix</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - frontend/knowbase-ui/src/views/IngestView.vue — preview and upload calls
    - frontend/knowbase-ui/src/api/ingest.js — uploadDocument, parsePreview
    - frontend/knowbase-ui/src/api/chunk.js — chunk preview
    - frontend/knowbase-ui/src/api/library.js — library CRUD
    - knowbase-service/src/main/java/com/knowbase/ingest/controller/DocumentController.java
    - knowbase-service/src/main/java/com/knowbase/library/controller/VectorLibraryController.java
    - knowbase-service/src/main/java/com/knowbase/platform/controller/IndexAdminController.java — chunk-preview, rebuild-library
    - .planning/phases/01-ingest-pipeline/01-PATTERNS.md — §4 API endpoint table seeds
  </read_first>
  <action>
    Write §4 阶段·类·API 对照 (PIPE-03):

    4.1 Combined matrix table columns: 阶段 | HTTP Method + Path | Controller#method | 后端服务类 | 前端 api/*.js 函数 | UI 入口组件

    Include minimum endpoints:
    - Library: GET/POST/PUT /api/v1/vector-libraries (CreateLibraryWizard, EditLibrarySettingsDrawer, IngestView)
    - Upload constraints: GET /documents/upload-constraints
    - Parse preview: POST /documents/parse-preview
    - Chunk preview: POST /index/chunk-preview
    - Upload: POST /documents/upload (+ batch)
    - Approve index: POST /documents/{docId}/approve-index
    - List chunks: GET /documents/{docId}/chunks (DocumentChunksView quality anchor)
    - Rebuild library: POST /index/rebuild-library (partial manual workaround for target soft-lock)

    4.2 Component responsibility table (≥10 rows): Component | Responsibility | File — covering library config, upload, parse, index, preview, chunk filter, metadata builder, coordinator, frontend ingest entry.

    4.3 Annotate preview API fork explicitly: IngestView calls parsePreview then fetchChunkPreview — two APIs, neither persists chunks. Link to Phase 4 PARITY requirements in backlog pointer (full PARITY detail in Plan 03 appendix).

    Verify every frontend api path matches controller @RequestMapping — use PATTERNS §4 seeds as baseline, confirm against source.
  </action>
  <acceptance_criteria>
    - ≥8 API rows with Method, Path, Controller, frontend function, UI component
    - Both preview endpoints documented separately from upload
    - documentMetadata param documented on upload row only
    - Component table ≥10 rows with file paths
  </acceptance_criteria>
  <verify>
    <automated>rg -c "DocumentController|IndexAdminController|VectorLibraryController|parse-preview|chunk-preview|/documents/upload" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Developer can map any ingest UI action to exact HTTP endpoint and consuming service class.</done>
</task>

<task type="auto">
  <name>Task 3: Preview vs index current gaps (D-14)</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - frontend/knowbase-ui/src/views/IngestView.vue — overrideChunkEnabled, buildChunkPreviewBody (~lines 517–531, 1132)
    - frontend/knowbase-ui/src/api/ingest.js — uploadParams (no chunkSize)
    - knowbase-service/src/main/java/com/knowbase/vector/service/ChunkPreviewService.java
    - knowbase-service/src/main/java/com/knowbase/ingest/service/ParsePreviewService.java
    - knowbase-service/src/main/java/com/knowbase/vector/service/IndexingService.java
    - .planning/codebase/CONCERNS.md — preview inconsistency entries
    - .planning/phases/01-ingest-pipeline/01-RESEARCH.md — Anti-Patterns, Current Gaps table
  </read_first>
  <action>
    Write §4.4 (or §3.5) 「当前差距：预览 vs 入库」 per D-14:

    Side-by-side table columns: 维度 | 预览路径 | 正式入库路径 | 差距
    Rows minimum:
    - 解析来源: ParsePreviewService extract-only vs stored parsed.txt from DocumentPipelineService
    - 规范化/清洗顺序: ChunkPreviewService re-applies vs pipeline pre-cleaned text then IndexingService cleans again
    - 分块参数: overrideChunk in chunk-preview body vs libraryConfigResolver.chunkingFor(libraryId) only
    - UI copy mismatch: IngestView claims override applies to ingest but uploadDocument sends no chunk overrides
    - 块数预期: preview totalChunks may differ from post-index chunk count

    State target-state (Phase 4 PARITY): preview rules = index rules, overrideChunk persists or is removed.
    Cite file:line evidence for overrideChunk preview-only (IngestView.vue + ingest.js).
    Do NOT write full Appendix A four-anchor table here — reference 附录 A for DocumentPipelineService and IndexingService anchor rows (Plan 03).
  </action>
  <acceptance_criteria>
    - overrideChunk preview-only gap documented with code anchors
    - ParsePreviewService vs IndexingService path difference explicit
    - Phase 4 PARITY linked as remediation target
    - Does not claim preview equals index today
  </acceptance_criteria>
  <verify>
    <automated>rg -c "overrideChunk|ParsePreviewService|ChunkPreviewService|PARITY|预览" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Ops understands why preview block count may differ from indexed count; dev knows which services diverge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc → runtime behavior | Incorrect API paths cause integration debugging waste |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | API matrix paths | mitigate | Verify each path against controller @GetMapping/@PostMapping annotations in source |
| T-01-04 | Tampering | Stage ordering narrative | mitigate | Match stage order to DocumentIngestor → DocumentPipelineService → IndexingService call chain |
</threat_model>

<verification>
- Trace exercise: pick POST /documents/upload row → follow to document_chunk in §3 stage table
- Preview fork documented without conflating ParsePreviewService and IndexingService
- All PIPE-02 stages present; all PIPE-03 API rows have frontend + backend columns
</verification>

<success_criteria>
- PIPE-02 and PIPE-03 requirements satisfied from §3–§4
- D-13 and D-14 addressed
- ROADMAP success criterion partially met: ingest path traceable; quality/appendix completed in Plan 03
</success_criteria>

<output>
Create `.planning/phases/01-ingest-pipeline/01-02-SUMMARY.md` when done
</output>

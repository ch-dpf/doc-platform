---
phase: 01-ingest-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/docs/INGEST-PIPELINE.md
autonomous: true
requirements:
  - PIPE-01
must_haves:
  truths:
    - "Reader can find separate ops and dev entry points in INGEST-PIPELINE.md"
    - "Document states target-state-first narrative with v1 scope boundary (D-02)"
    - "Library type decision tree guides vertical vs general library choice by semantic axis (D-08–D-12)"
    - "Three-tier config matrix maps rules to system/library-default/ingest-override tiers (D-07)"
    - "Library creation flow traces wizard fields through config_json to LibraryConfigResolver methods (PIPE-01)"
  artifacts:
    - path: ".planning/docs/INGEST-PIPELINE.md"
      provides: "Phase 1 doc scaffold through §5 config matrix and §2 library creation"
      min_lines: 200
      contains: "PIPE-01"
  key_links:
    - from: ".planning/docs/INGEST-PIPELINE.md"
      to: "CreateLibraryWizard.vue"
      via: "wizard step ↔ config_json field table"
      pattern: "CreateLibraryWizard|WIZARD_STEPS|config_json"
    - from: ".planning/docs/INGEST-PIPELINE.md"
      to: "LibraryConfigResolver.java"
      via: "*For(libraryId) resolver method map"
      pattern: "LibraryConfigResolver|parseOptionsFor|chunkingFor"
---

<objective>
Create the INGEST-PIPELINE.md foundation: dual-audience reader guide, target-state vision, library-type decision tree, three-tier config matrix, and PIPE-01 library-creation flow (wizard → config_json → LibraryConfigResolver).

Purpose: Establishes the target-state narrative baseline (D-01) and the configuration boundary model ops/dev must share before ingest-stage documentation.
Output: `.planning/docs/INGEST-PIPELINE.md` sections 1–2, 5, 6 (decision tree), partial §3 scaffold; no ingest pipeline body yet (Plan 02).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ingest-pipeline/01-CONTEXT.md
@.planning/phases/01-ingest-pipeline/01-RESEARCH.md
@.planning/phases/01-ingest-pipeline/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
</context>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| `.planning/docs/INGEST-PIPELINE.md` | 01–03 | Primary deliverable — full target-state ingest pipeline reference |
| `.planning/STATE.md` | 03 | Phase 1 completion status update |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold doc with reader guide and vision</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - .planning/phases/01-ingest-pipeline/01-PATTERNS.md — doc-root front matter and section naming
    - .planning/phases/01-ingest-pipeline/01-CONTEXT.md — D-01, D-02 locked decisions
    - .planning/codebase/ARCHITECTURE.md — component responsibility table pattern
  </read_first>
  <action>
    Create `.planning/docs/INGEST-PIPELINE.md` with YAML front matter (`last_mapped_commit`, `analysis_date: 2026-06-10`, `focus: ingest-pipeline`).

    Write §1 范围与读者指南 containing:
    - Dual TOC anchors: `#ops-guide` (运营入口 → 决策树、质量准则、反模式) and `#dev-reference` (开发入口 → 建库/入库流程、配置矩阵、附录 A)
    - Milestone core value quote from PROJECT.md
    - Target-state vs v1 boundary per D-02: vision includes general mixed library as first-class; v1 delivers doc + recall-layer quality criteria + anti-patterns; RAG answer-rate is north star only, not v1 engineering acceptance
    - Traceability table mapping PIPE-01/02/03 to section placeholders (§2, §3, §4)
    - Explicit note: Phase 1 is documentation-only; no application code changes

    Add placeholder headings for §3 单文档入库流程, §7 分块质量准则, §8 反模式, 附录 A/B — leave body as "（Plan 02/03 填充）" one-liners so later plans have insertion points.

    Do NOT duplicate per-file-type matrix (D-17) — add single cross-ref to future `.planning/docs/FILE-TYPE-PROCESSING.md` (Phase 2).
  </action>
  <acceptance_criteria>
    - File exists with front matter and both `#ops-guide` / `#dev-reference` anchors
    - D-01 target-state-first and D-02 v1 boundary stated explicitly
    - PIPE-01/02/03 traceability table present
    - No per-type processing detail beyond Phase 2 reference
  </acceptance_criteria>
  <verify>
    <automated>rg -c "#ops-guide|#dev-reference|PIPE-01|PIPE-02|PIPE-03" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Scaffold exists; ops and dev can navigate to intended sections; v1 scope boundary is unambiguous.</done>
</task>

<task type="auto">
  <name>Task 2: Library type decision tree and three-tier config matrix</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - .planning/phases/01-ingest-pipeline/01-CONTEXT.md — D-03 through D-12
    - .planning/phases/01-ingest-pipeline/01-RESEARCH.md — Pattern 1 resolver map, system-tier fields
    - .planning/phases/01-ingest-pipeline/01-PATTERNS.md — §5 matrix structure, §6 decision tree mermaid
    - frontend/knowbase-ui/src/utils/libraryDefaults.js — defaultLibraryConfig field paths
    - knowbase-service/src/main/java/com/knowbase/library/service/LibraryConfigResolver.java — *For methods
  </read_first>
  <action>
    Write §6 库类型选型决策树 per D-08–D-12:
    - Mermaid flowchart: 业务场景 → {同质语义+同问答模式?} → 垂直专用库 | 通用混合库 → MIME感知默认 + documentMetadata语义标签(通用) → 向导微调 → Phase 3 预设占位（链接 ROADMAP Phase 3，不展开预设定义）
    - Heuristic bullet list: 周报xlsx+周报pdf同库(D-11); 周报xlsx vs 报销xlsx拆库(D-10); 不以扩展名为唯一依据
    - State general mixed library is first-class target (D-09) with MIME auto-default as future implementation

    Write §5 三层配置矩阵 per D-03–D-07:
    - Table columns: 规则项 | 配置路径 | 系统级 | 库默认 | 采集覆盖(目标态) | 现状
    - Cell values use: 必须 / 默认 / 可覆盖 / 禁止
    - System tier (D-03): max-file-size, batch limits, OCR engine availability, global embedding fallback — cite application.yml / IngestProperties keys
    - Library must-unify tier (D-04): embedding model/dimension, hybrid/rerank, metadataFilterFields, capacity, version policy, supported types ceiling
    - Library defaults overridable at ingest (D-05): parsing, cleaning, chunking — mark 现状 as 库级 only
    - Ingest tier (D-06): documentMetadata = semantic tags only (retrieval filter); ingest profile OCR/chunk override documented as 目标态可覆盖 with 现状 未实现
    - Include resolver method column or sub-table mapping config paths to LibraryConfigResolver.*For(libraryId) per RESEARCH Pattern 1 (minimum 10 rows covering parsing, cleaning, chunking, embedding, retrieval, ingestAccess, governance)
    - Align dot-path keys with libraryConfig.js CONFIG_FIELD_SPECS / REINDEX_FIELDS

    End §5 with short 「当前差距」 noting whole pipeline locked at library tier today (preview of Appendix A, full detail deferred to Plan 03).
  </action>
  <acceptance_criteria>
    - Decision tree mermaid renders valid syntax (flowchart TD)
    - Config matrix has ≥10 rule rows with all four tier columns populated
    - D-06 explicitly states documentMetadata does not drive parse/chunk pipeline in v1
    - Phase 3 preset referenced but not defined
    - No TYPE-01–05 per-type detail (Phase 2 deferral)
  </acceptance_criteria>
  <verify>
    <automated>rg -c "flowchart|同质语义|系统级|库默认|采集覆盖|documentMetadata" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Ops can choose library type by semantic axis; dev/ops share config tier boundaries in one matrix table.</done>
</task>

<task type="auto">
  <name>Task 3: PIPE-01 library creation flow</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - frontend/knowbase-ui/src/components/CreateLibraryWizard.vue — WIZARD_STEPS
    - frontend/knowbase-ui/src/utils/libraryDefaults.js — buildCreatePayload, defaultLibraryConfig
    - frontend/knowbase-ui/src/api/library.js — createVectorLibrary
    - knowbase-service/src/main/java/com/knowbase/library/controller/VectorLibraryController.java — POST create
    - knowbase-service/src/main/java/com/knowbase/library/service/VectorLibraryService.java — create persists config_json
    - knowbase-service/src/main/java/com/knowbase/library/service/LibraryConfigResolver.java — config(libraryId) and stage methods
    - .planning/phases/01-ingest-pipeline/01-PATTERNS.md — §2 wizard mapping
  </read_first>
  <action>
    Write §2 建库流程 (PIPE-01):

    2.1 Mermaid flowchart: CreateLibraryWizard → POST /api/v1/vector-libraries → VectorLibraryService.create → vector_library.config_json → LibraryConfigResolver used at runtime by pipeline services. Include EditLibrarySettingsDrawer PUT path as secondary branch.

    2.2 Wizard step ↔ config_json field table — map each WIZARD_STEPS title to top-level and nested config paths (ingestAccess, parsing, cleaning, chunking*, embedding*, retrieval, governance). Use libraryDefaults.js defaultLibraryConfig as 库默认 column exemplar values.

    2.3 config_json → LibraryConfigResolver 生效点 table: list each *For(libraryId) method, config path group, and primary consumer class (DocumentParseService, DocumentPipelineService, IndexingService, ChunkPreviewService, LibraryEmbeddingService, etc.) — copy verified rows from RESEARCH Pattern 1 resolver map.

    2.4 Numbered data-flow (5–8 steps): form submit → API → JSONB persist → first ingest read via resolver.

    §2 末尾「当前差距」 subsection (brief — full Appendix A in Plan 03):
    - lockPipeline hard lock when documentCount>0 || chunkCount>0 (VectorLibraryConfigMerger)
    - No library-type axis in wizard UI yet (decision tree is doc-only until Phase 3)
    - Target soft-lock + full-library reindex (D-15) stated as target, not current

    Add 代码锚点 bullet list linking canonical Java/Vue paths from 01-CONTEXT.md canonical_refs.
  </action>
  <acceptance_criteria>
    - PIPE-01 requirement fully addressable from §2 alone
    - Wizard steps map to real config_json paths verified against libraryDefaults.js
    - Resolver method table covers parsing, cleaning, chunking, embedding, retrieval
    - Mermaid flowchart for library creation present
    - lockPipeline gap noted without claiming soft-lock exists
  </acceptance_criteria>
  <verify>
    <automated>rg -c "CreateLibraryWizard|config_json|LibraryConfigResolver|VectorLibraryService|WIZARD_STEPS|lockPipeline" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Newcomer can trace 建库 from wizard field to resolver method that applies at ingest time.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc author → codebase | Documentation must accurately cite classes/APIs; misdocumentation misleads ops into wrong settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | INGEST-PIPELINE.md config matrix | mitigate | Cross-check every resolver row against LibraryConfigResolver source and libraryDefaults.js paths before merge |
| T-01-02 | Information Disclosure | Documented tenantId gap | accept | Document as known limitation per RESEARCH Security Domain; no secrets in doc |
</threat_model>

<verification>
- §2 cites real wizard steps and API paths verifiable in codebase
- §5 matrix rows align with libraryConfig.js dot-paths
- Decision tree honors semantic-axis rule (D-11) with concrete 周报/报销 example
</verification>

<success_criteria>
- PIPE-01 satisfied: wizard → config_json → LibraryConfigResolver path documented with tables and mermaid
- D-03–D-12 decisions cited in relevant sections
- Document ≥200 lines with scaffold for Plans 02–03
</success_criteria>

<output>
Create `.planning/phases/01-ingest-pipeline/01-01-SUMMARY.md` when done
</output>

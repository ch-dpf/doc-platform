---
phase: 01-ingest-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - .planning/docs/INGEST-PIPELINE.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
must_haves:
  truths:
    - "General chunk quality criteria state RAG north star and v1 recall-layer acceptance (D-16)"
    - "Anti-pattern gallery includes 杜鹏飞 weekly-report xlsx, scan PDF OCR-off, header-only chunks (D-18)"
    - "Appendix A documents four mandatory anchor gaps: DocumentPipelineService, IndexingService, IngestView.vue, VectorLibraryConfigMerger"
    - "Appendix B lists backlog: PARITY, GATE-01/02, ingest profile, soft-lock, MIME auto-default (D-19, deferred items)"
    - "Newcomer can trace 建库 → document_chunk per ROADMAP success criterion"
  artifacts:
    - path: ".planning/docs/INGEST-PIPELINE.md"
      provides: "Complete Phase 1 deliverable with §7–§8 and appendices"
      min_lines: 400
      contains: "附录 A"
    - path: ".planning/STATE.md"
      provides: "Phase 1 marked complete"
  key_links:
    - from: ".planning/docs/INGEST-PIPELINE.md"
      to: "ChunkPreviewServiceTest.java"
      via: "杜鹏飞 anti-pattern sample citation"
      pattern: "杜鹏飞|ChunkPreviewServiceTest"
    - from: ".planning/docs/INGEST-PIPELINE.md"
      to: "VectorLibraryConfigMerger.java"
      via: "Appendix A lockPipeline hard lock"
      pattern: "VectorLibraryConfigMerger|lockPipeline"
---

<objective>
Complete INGEST-PIPELINE.md with quality criteria, anti-patterns, appendices (current gaps + backlog), final traceability verification, and STATE.md phase completion.

Purpose: Closes Phase 1 with ops-readable quality guidance and developer-readable gap analysis — fulfilling D-16–D-19 and ROADMAP「新人可据文档从建库追到 document_chunk」.
Output: Final `.planning/docs/INGEST-PIPELINE.md`; updated `.planning/STATE.md`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ingest-pipeline/01-CONTEXT.md
@.planning/phases/01-ingest-pipeline/01-RESEARCH.md
@.planning/phases/01-ingest-pipeline/01-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/docs/INGEST-PIPELINE.md
@.planning/ROADMAP.md
</context>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| `.planning/docs/INGEST-PIPELINE.md` §7–§8, 附录 A–B | 03 | Quality, anti-patterns, gaps, backlog |
| `.planning/STATE.md` | 03 | Phase 1 status → complete |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Quality criteria and anti-pattern gallery</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - .planning/phases/01-ingest-pipeline/01-CONTEXT.md — D-16, D-17, D-18
    - knowbase-service/src/main/java/com/knowbase/vector/chunk/IndexingChunkFilter.java
    - knowbase-service/src/test/java/com/knowbase/vector/service/ChunkPreviewServiceTest.java — 杜鹏飞 fixture
    - .planning/codebase/CONCERNS.md — Tech Debt entry format
    - .planning/phases/01-ingest-pipeline/01-RESEARCH.md — Pitfall 4 杜鹏飞 chunk count, anti-pattern table
  </read_first>
  <action>
    Write §7 分块质量准则 per D-16, D-17:
    - North star: RAG 可答率 — block contains sufficient self-contained facts to support correct answers (document only, not v1 test gate)
    - v1 acceptance layer: 检索可召回 — bullets: 块自洽、非纯表头、关键事实不无故跨块断裂、预览=入库（目标态，现状见 §4.4）
    - IndexingChunkFilter role: remove header-only chunks; fallback keeps all if filter would zero out
    - ChunkMetadataBuilder fields: mimeType, fileName, docType, custom semantic tags for retrieval narrowing
    - Single paragraph cross-ref to `.planning/docs/FILE-TYPE-PROCESSING.md` (Phase 2) for per-type recommendations — do NOT duplicate TYPE-01–05 content

    Write §8 反模式对照 per D-18 using CONCERNS-style table:
    Columns: 反模式 | 错误设定/行为 | 症状 | 代码锚点 | 正确做法（目标态/Phase引用）

    Required rows (minimum 4):
    1. 预览≠入库 — overrideChunkEnabled / misleading UI copy — preview N blocks, index M blocks — IngestView.vue, IndexingService — Phase 4 PARITY
    2. 杜鹏飞周报 xlsx — wrong chunk strategy or ignoring header filter — retrieval hits headers or misses rows — ChunkPreviewServiceTest fixture — paragraph-first + text-only; cite test params (chunkSize=500: rawTotalChunks=4, filteredOutCount=1, totalChunks=3) AND footnote user-verified 4-chunk recall with production params (RESEARCH Assumption A1)
    3. 扫描 PDF OCR 关闭 — parsing.ocrEnabled=false on image PDF — empty/garbled chunks — DocumentParseService — enable OCR + tessdata
    4. 异质语义混库 — 周报+报销 same library — retrieval noise — CONTEXT D-10 — split by semantic axis per §6 decision tree
    5. tableExtraction structured on Excel — structured mode on xlsx — no benefit, wrong expectation — DocumentParseService HTML-only — Phase 2 TYPE-03 cross-ref

    Each row includes Files: backtick paths.
  </action>
  <acceptance_criteria>
    - D-16 north star vs v1 recall layer clearly separated
    - 杜鹏飞 sample cites ChunkPreviewServiceTest with parameter footnote for count discrepancy
    - Phase 2 FILE-TYPE-PROCESSING.md referenced, not duplicated
    - ≥4 anti-pattern rows with code anchors
  </acceptance_criteria>
  <verify>
    <automated>rg -c "RAG|检索可召回|杜鹏飞|IndexingChunkFilter|反模式|FILE-TYPE-PROCESSING" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Ops can recognize common misconfiguration patterns; quality north star and v1 bar are explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Appendix A current gaps and Appendix B backlog</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - knowbase-service/src/main/java/com/knowbase/ingest/service/DocumentPipelineService.java
    - knowbase-service/src/main/java/com/knowbase/vector/service/IndexingService.java
    - frontend/knowbase-ui/src/views/IngestView.vue
    - knowbase-service/src/main/java/com/knowbase/library/config/VectorLibraryConfigMerger.java
    - knowbase-service/src/main/java/com/knowbase/library/service/VectorLibraryService.java — lockPipeline trigger
    - frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue — lockPipeline UI
    - .planning/phases/01-ingest-pipeline/01-CONTEXT.md — deferred ideas, D-15, Claude's Discretion four anchors
    - .planning/phases/01-ingest-pipeline/01-PATTERNS.md — 附录 A/B templates
  </read_first>
  <action>
    Write 附录 A: 当前差距详表 — mandatory four-anchor structure per Claude's Discretion:

    | 锚点 | 现状 | 目标态 | 代码证据 | 关联 Phase/Backlog |

    Rows for:
    1. DocumentPipelineService — library-only parsing/cleaning via libraryConfigResolver.*For(libraryId); no ingest profile override
    2. IndexingService — chunkingFor(libraryId) single source; no overrideChunk; double cleaning of parsed.txt
    3. IngestView.vue — overrideChunk preview-only; lockPipeline disables controls; documentMetadata semantic only
    4. VectorLibraryConfigMerger — hard lock discards parsing/cleaning/chunking/embedding updates when lockPipelineConfig=true; cite mergeSafeFields early return

    Expand each anchor with CONCERNS-style Issue/Files/Impact/Fix approach bullets.

    Write 附录 B: Backlog 与后续 Phase 引用:
    - Phase 4 PARITY-01–04: preview=index parity
    - Phase 2 TYPE-01–05: per-file-type matrix
    - Phase 3 PRESET-01–04: library presets in wizard
    - Phase 5 CFG-01–02: config diff UX
    - v2 GATE-01/02 quality gates (D-19 — define criteria, no v1 implementation promise)
    - Deferred: ingest profile persistence, MIME auto-default engine, soft-lock + full-library reindex task, structured dual-track QueryRouter

    Write 附录 C (optional field path index): condensed CONFIG_FIELD_SPECS table from PATTERNS 附录 B — path | 中文标签 | 影响重索引 | Wizard步骤 — minimum 15 rows aligned with libraryConfig.js dot-paths. If doc length exceeds ~600 lines, appendix C may be abbreviated to top 15 reindex fields only.

    Remove all "（Plan 02/03 填充）" placeholders from prior plans.
  </action>
  <acceptance_criteria>
    - All four anchor classes have dedicated Appendix A rows with code evidence
    - lockPipeline hard vs soft-lock (D-15) documented accurately
    - Deferred ideas from CONTEXT.md appear in Appendix B, not as promised v1 features
    - GATE-01/02 listed as backlog only
    - No remaining placeholder sections
  </acceptance_criteria>
  <verify>
    <automated>rg -c "附录 A|DocumentPipelineService|IndexingService|IngestView|VectorLibraryConfigMerger|附录 B|GATE-01" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Developer has single appendix for all target-vs-current gaps; backlog items trace to future phases.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification and STATE update</name>
  <files>.planning/docs/INGEST-PIPELINE.md, .planning/STATE.md</files>
  <read_first>
    - .planning/ROADMAP.md — Phase 1 success criteria
    - .planning/REQUIREMENTS.md — PIPE-01/02/03 definitions
    - .planning/docs/INGEST-PIPELINE.md — full draft from Plans 01–02 and Tasks 1–2
  </read_first>
  <action>
    Final editorial pass on INGEST-PIPELINE.md:
    - Add/update traceability table at doc end: Requirement | Section | Status — mark PIPE-01→§2, PIPE-02→§3, PIPE-03→§4 as Covered
    - Confirm ≥2 mermaid diagrams (library creation flowchart + ingest sequenceDiagram; decision tree counts as third)
    - Confirm config matrix in §5 has 必须/默认/可覆盖/禁止 cell values
    - Run newcomer trace checklist inline as §9 验收清单 (5 numbered steps): 建库字段→config_json→上传API→管道阶段→document_chunk — each step cites section anchor
    - Set front matter last_mapped_commit to current git short SHA (run git rev-parse --short HEAD)
    - Fix broken internal cross-links

    Update `.planning/STATE.md`:
    - Phase 1 row status: Context gathered → Complete (or「文档已交付」)
    - Current focus: Phase 2 — 文件类型设定矩阵
    - Add session note: Phase 1 INGEST-PIPELINE.md delivered {date}
    - Progress table Phase 1 status updated

    Do NOT modify knowbase-service or frontend source files.
  </action>
  <acceptance_criteria>
    - All PIPE-01/02/03 marked Covered in traceability table
    - §9 trace exercise has 5 steps with section references
    - STATE.md reflects Phase 1 complete and Phase 2 next
    - Document ≥400 lines, no TODO placeholders
    - last_mapped_commit populated
  </acceptance_criteria>
  <verify>
    <automated>rg -c "PIPE-01|PIPE-02|PIPE-03|sequenceDiagram|flowchart|验收清单" .planning/docs/INGEST-PIPELINE.md | rg -v "^#" | wc -l &amp;&amp; rg "Phase 1|INGEST-PIPELINE" .planning/STATE.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Phase 1 deliverable complete; STATE reflects milestone progress; ROADMAP success criterion documented as §9 checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc completeness → milestone gate | Missing appendix anchors allow false confidence in current system capabilities |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Repudiation | Gap appendix | mitigate | Four-anchor checklist enforced; grep verify each class name in 附录 A |
| T-01-06 | Information Disclosure | Anti-pattern samples | accept | Use test fixture data only; no production PII beyond anonymized 杜鹏飞 sample already in tests |
</threat_model>

<verification>
- Manual newcomer trace: follow §9 checklist without opening Java/Vue source
- PIPE-01/02/03 traceability table all Covered
- Deferred items (ingest profile, MIME engine, soft-lock) appear only in Appendix B, not as shipped features
- No application code files modified outside .planning/
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion met: 新人可据文档从「建库」追到 document_chunk 写入而无歧义
- All locked decisions D-01–D-19 represented in appropriate sections
- STATE.md updated; Phase 1 ready for `/gsd-verify-work 1` or Phase 2 planning
</success_criteria>

<output>
Create `.planning/phases/01-ingest-pipeline/01-03-SUMMARY.md` when done
</output>

---
phase: 03-library-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/knowbase-ui/package.json
  - frontend/knowbase-ui/vite.config.js
  - frontend/knowbase-ui/src/utils/appendixAPresetAudit.js
  - frontend/knowbase-ui/src/utils/libraryPresets.test.js
  - frontend/knowbase-ui/src/utils/libraryPresets.js
  - knowbase-service/src/test/java/com/knowbase/library/config/VectorLibraryConfigPresetTest.java
autonomous: true
requirements:
  - PRESET-01
  - PRESET-02
  - PRESET-03
must_haves:
  truths:
    - "4 种命名预设存在且各有 summary 一句话说明（PRESET-01）"
    - "applyLibraryPreset 后 parsing/cleaning/chunking 子树字段齐全且非 undefined（PRESET-02）"
    - "weekly-report-excel / scan-reimbursement / policy-longform 与 ROADMAP 三锚点及附录 A 关键字段一致"
    - "建库 apply 写入 libraryPresetId 且 supportedFileTypes 与预设匹配（PRESET-03）"
    - "JsonSupport 往返序列化保留 libraryPresetId"
  artifacts:
    - path: "frontend/knowbase-ui/src/utils/libraryPresets.test.js"
      provides: "预设定义、套用、附录 A 对齐单测"
    - path: "frontend/knowbase-ui/src/utils/appendixAPresetAudit.js"
      provides: "附录 A / §2 锚点期望值表"
    - path: "knowbase-service/src/test/java/com/knowbase/library/config/VectorLibraryConfigPresetTest.java"
      provides: "config_json libraryPresetId 往返测试"
  key_links:
    - from: "libraryPresets.test.js"
      to: "appendixAPresetAudit.js"
      via: "ROADMAP 三锚点断言"
      pattern: "weekly-report-excel|scan-reimbursement|policy-longform"
    - from: "libraryPresets.test.js"
      to: "libraryPresets.js"
      via: "applyLibraryPreset / getLibraryPresetById"
      pattern: "applyLibraryPreset"
---

<objective>
为 Phase 3 已交付的 `libraryPresets.js` 与 `VectorLibraryConfig.libraryPresetId` 建立自动化验收：预设完整性、附录 A 对齐审计、建库套用行为与后端 JSON 往返。

Purpose: 9c3101d 已实现 UI/数据模型，本 plan 不重复建向导，只补测试与对齐修正。
Output: vitest 单测套件 + 附录 A 审计模块 + Java 往返测试；若审计失败则最小修正 `libraryPresets.js` overrides。
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/docs/FILE-TYPE-PROCESSING.md
@frontend/knowbase-ui/src/utils/libraryPresets.js
@frontend/knowbase-ui/src/utils/libraryDefaults.js
@knowbase-service/src/main/java/com/knowbase/library/config/VectorLibraryConfig.java
@knowbase-service/src/main/java/com/knowbase/platform/JsonSupport.java
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 附录 A 审计模块 + vitest 预设单测</name>
  <files>frontend/knowbase-ui/package.json, frontend/knowbase-ui/vite.config.js, frontend/knowbase-ui/src/utils/appendixAPresetAudit.js, frontend/knowbase-ui/src/utils/libraryPresets.test.js</files>
  <behavior>
    - LIBRARY_PRESETS 长度 === 4，id 集合含 weekly-report-excel / policy-longform / scan-reimbursement / general-mixed
    - 每预设 summary 非空字符串（PRESET-02 一句话说明）
    - applyLibraryPreset(default, id) 后 config.parsing / config.cleaning / config.chunkingStrategy / config.chunkSize / config.chunkOverlap 均存在
    - weekly-report-excel: chunkingStrategy paragraph-first, parsing.tableExtraction text-only, supportedFileTypes 仅 excel（TYPE-03 / ROADMAP 周报 xlsx）
    - scan-reimbursement: parsing.ocrEnabled true, supportedFileTypes 含 pdf（TYPE-01 扫描 pdf）
    - policy-longform: chunkingStrategy heading-level, parsing.tableExtraction structured, supportedFileTypes 含 word（TYPE-02 制度 docx）
    - general-mixed: supportedFileTypes 五种齐全；chunkingStrategy paragraph-first；parsing.ocrEnabled false
    - applyLibraryPreset 设置 libraryPresetId === preset.id；未知 id 返回 libraryPresetId custom
    - resolveLibraryPresetLabel 对四套 preset 返回中文名；手改 chunkingStrategy 后返回「自定义」
  </behavior>
  <action>
    在 knowbase-ui 添加 vitest（devDependency）与 `test` script；vite.config.js 启用 test.environment node。
    新建 appendixAPresetAudit.js：导出 APPENDIX_A_EXPECTATIONS（按 fileTypeKey 的 parsing/chunking 片段，来源 FILE-TYPE-PROCESSING.md 附录 A L233–240）与 ROADMAP_ANCHOR_EXPECTATIONS（三锚点 presetId → 关键字段，来源 §2 L88/L101/L111）。
    新建 libraryPresets.test.js：import LIBRARY_PRESETS、applyLibraryPreset、resolveLibraryPresetLabel、defaultLibraryConfig；用审计表断言每 preset 套用结果。
    不修改 CreateLibraryWizard.vue（PRESET-03 UI 已在 9c3101d）；仅测 applyLibraryPreset 契约。
    若测试暴露 overrides 与附录 A/§2 冲突，在 Task 2 修正 libraryPresets.js（禁止删预设或减字段）。
  </action>
  <verify>
    <automated>cd frontend/knowbase-ui && npm install && npm test</automated>
  </verify>
  <done>vitest 全绿；4 预设 + 附录 A 三锚点断言通过</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 预设对齐修正（仅测试失败时）+ 后端 libraryPresetId 往返测试</name>
  <files>frontend/knowbase-ui/src/utils/libraryPresets.js, knowbase-service/src/test/java/com/knowbase/library/config/VectorLibraryConfigPresetTest.java</files>
  <behavior>
    - 若 Task 1 失败：libraryPresets.js overrides 最小 diff 使审计通过，不改动 preset id/name 语义
    - VectorLibraryConfigPresetTest: 设置 libraryPresetId weekly-report-excel → JsonSupport.toJson → parseLibraryConfig → getter 相同
    - 空 config_json 解析后 libraryPresetId 为 null（不抛异常）
  </behavior>
  <action>
    仅当 Task 1 审计失败时编辑 libraryPresets.js overrides（例如 policy-longform 缺 cleaning 字段、scan 库缺 removeHeaderFooter 等）；保持 mergeNested 逻辑不变。
    新建 VectorLibraryConfigPresetTest.java：JUnit5，无 Spring；直接 JsonSupport 序列化/反序列化 VectorLibraryConfig；覆盖 weekly-report-excel 与 custom 两例。
    不新增 VectorLibraryConfig 字段（9c3101d 已有 libraryPresetId）。
  </action>
  <verify>
    <automated>cd frontend/knowbase-ui && npm test; cd knowbase-service && mvn -q test -Dtest=VectorLibraryConfigPresetTest</automated>
  </verify>
  <done>前后端预设相关测试全绿；libraryPresets.js 与附录 A/ROADMAP 锚点一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试夹具 → libraryPresets.js | 只读生产预设定义；修正仅限 overrides 数值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm vitest 安装 | mitigate | 使用 npmjs 官方 vitest 包；package-lock 锁定版本 |
| T-03-SC | Tampering | npm install | mitigate | slopcheck + 仅添加 vitest/@vitest/* 已知包 |
</threat_model>

<verification>
- `npm test` 在 knowbase-ui 通过
- `mvn test -Dtest=VectorLibraryConfigPresetTest` 通过
- 不修改 CreateLibraryWizard / EditLibrarySettingsDrawer（留给 03-02/已交付代码）
</verification>

<success_criteria>
PRESET-01/02/03 行为有自动化证明；附录 A 与 ROADMAP 三锚点审计可重复运行；libraryPresetId JSON 往返可靠。
</success_criteria>

<output>
Create `.planning/phases/03-library-presets/03-01-SUMMARY.md` when done
</output>

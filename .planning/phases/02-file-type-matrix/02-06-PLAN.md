---
phase: 02-file-type-matrix
plan: 06
type: execute
wave: 3
depends_on:
  - 02-05
files_modified:
  - .planning/docs/FILE-TYPE-PROCESSING.md
autonomous: true
gap_closure: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
must_haves:
  truths:
    - "Appendix C header accurately scopes fields relative to REINDEX_FIELDS (WR-01)"
    - "minParagraphLength code anchor points to application.yml L158 / VectorLibraryConfig.java default (WR-02)"
    - "Non-REINDEX_FIELDS rows carry explicit footnotes"
  artifacts:
    - path: ".planning/docs/FILE-TYPE-PROCESSING.md"
      provides: "Accurate Appendix C dev field index"
      contains: "主矩阵相关字段"
  key_links:
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md 附录 C"
      to: "application.yml chunking.min-paragraph-length"
      via: "minParagraphLength code anchor"
      pattern: "application\.yml.*158|min-paragraph-length"
---

<objective>
Correct Appendix C REINDEX_FIELDS scope claims and fix the minParagraphLength code anchor so dev readers trace the right system default source.

Purpose: Closes WR-01/WR-02 / verification gap #3 — Phase 5 CFG-01 consumers must not misread field scope or system defaults.
Output: Revised 附录 C intro, footnotes, and minParagraphLength row in `.planning/docs/FILE-TYPE-PROCESSING.md`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-file-type-matrix/02-VERIFICATION.md
@.planning/phases/02-file-type-matrix/02-REVIEW.md
@.planning/phases/02-file-type-matrix/02-05-SUMMARY.md
@.planning/docs/FILE-TYPE-PROCESSING.md
@frontend/knowbase-ui/src/utils/libraryConfig.js
@knowbase-service/src/main/resources/application.yml
@knowbase-service/src/main/java/com/knowbase/library/domain/VectorLibraryConfig.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revise Appendix C scope header and REINDEX_FIELDS footnotes</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - .planning/docs/FILE-TYPE-PROCESSING.md L278–295 — Appendix C current intro and table
    - frontend/knowbase-ui/src/utils/libraryConfig.js L35–53 — REINDEX_FIELDS Set (actual members)
    - .planning/phases/02-file-type-matrix/02-REVIEW.md — WR-01
    - .planning/phases/02-file-type-matrix/02-VERIFICATION.md — gap missing items
  </read_first>
  <action>
    Fix Appendix C REINDEX_FIELDS overclaim per WR-01:

    1. Replace intro paragraph (L280) with scoped header:
       「**主矩阵相关字段子集** — 与 `libraryConfig.js` `REINDEX_FIELDS`（L35–53）及 `CONFIG_FIELD_SPECS` dot-path 对照；完整 diff 逻辑见 Phase 5 **CFG-01**。下表含主矩阵引用但**不在** `REINDEX_FIELDS` 的字段，脚注标注。」

    2. Add footnote markers on rows **not** in REINDEX_FIELDS Set:
       - `parsing.autoDetectEncoding` — footnote: 「不在 REINDEX_FIELDS；`diffLibraryConfig` 仍追踪 parsing 变更」
       - `parsing.defaultLanguage` — same pattern
       - `cleaning.removeHeaderFooter` — footnote: 「不在 REINDEX_FIELDS；见 `libraryConfig.js` diff L274」
       - `ingestAccess.supportedFileTypes` — footnote: 「不在 REINDEX_FIELDS；影响重索引列保持 **否**」

    3. For rows **in** REINDEX_FIELDS, keep「影响重索引: 是」without exception footnote: `parsing.ocrEnabled`, `parsing.tableExtraction`, `chunkingStrategy`, `chunkSize`, `chunkOverlap`, `minParagraphLength`, `cleaning.removeDuplicateParagraphs`, `textNormalizationEnabled`.

    4. Preserve `{#dev-field-paths}` anchor from Plan 05 — do not rename.

    Do NOT modify Java or JS source — doc-only per phase scope.
  </action>
  <acceptance_criteria>
    - Intro uses「主矩阵相关字段子集」not「与 REINDEX_FIELDS 对齐」without qualification
    - Four non-REINDEX_FIELDS rows have explicit footnotes
    - `ingestAccess.supportedFileTypes` row still shows 影响重索引 **否**
    - Table still ≥12 rows
  </acceptance_criteria>
  <verify>
    <automated>rg -v "^#" .planning/docs/FILE-TYPE-PROCESSING.md | rg -c "主矩阵相关字段" | ForEach-Object { if ([int]$_ -lt 1) { exit 1 } }; rg -v "^#" .planning/docs/FILE-TYPE-PROCESSING.md | rg -c "不在 REINDEX_FIELDS" | ForEach-Object { if ([int]$_ -lt 4) { exit 1 } }</automated>
  </verify>
  <done>WR-01 closed; Appendix C scope honest for Phase 5 consumers.</done>
</task>

<task type="auto">
  <name>Task 2: Fix minParagraphLength code anchor and cross-check §3 consistency</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - knowbase-service/src/main/resources/application.yml L152–160 — chunking.min-paragraph-length
    - knowbase-service/src/main/java/com/knowbase/library/domain/VectorLibraryConfig.java — minParagraphLength field default
    - knowbase-service/src/main/java/com/knowbase/library/service/VectorLibraryConfigFactory.java L75–83 — confirms chunkSize/chunkOverlap only
    - .planning/docs/FILE-TYPE-PROCESSING.md L186, L291 — current minParagraphLength citations
    - .planning/phases/02-file-type-matrix/02-REVIEW.md — WR-02 fix text
  </read_first>
  <action>
    Fix minParagraphLength anchor per WR-02:

    1. In Appendix C `minParagraphLength` row, replace code anchor `VectorLibraryConfigFactory.java L75–83` with:
       `application.yml` L158（`chunking.min-paragraph-length: 30`）；`VectorLibraryConfig.java` 字段默认 30

    2. Verify §3 defaults table and §3 code anchor bullet (L186) — if L186 lists Factory L75–83 for minParagraphLength, clarify Factory only defaults chunkSize/chunkOverlap; minParagraphLength system default from YAML + Java field.

    3. §2 matrix rows citing minParagraphLength **`30`** — no change needed unless they incorrectly cite Factory.

    4. Optional consistency: add one-line note in Appendix C that Factory L75–83 applies to chunkSize/chunkOverlap ≤0 fallback only (prevents future WR-02 recurrence).

    Preserve all other Appendix C row content and Resolver/UI bullets below the table.
  </action>
  <acceptance_criteria>
    - Appendix C minParagraphLength row cites application.yml L158 and VectorLibraryConfig.java
    - Appendix C minParagraphLength row does NOT cite VectorLibraryConfigFactory L75–83 as primary anchor
    - §3 code anchor section does not imply Factory sets minParagraphLength
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$lines=Get-Content '.planning/docs/FILE-TYPE-PROCESSING.md'; $inC=$false; foreach($l in $lines){if($l -match '## 附录 C'){$inC=$true}; if($inC -and $l -match 'minParagraphLength'){if($l -match 'VectorLibraryConfigFactory'){exit 1}; if($l -notmatch 'application\.yml'){exit 1}}}"</automated>
  </verify>
  <done>WR-02 closed; verification truth #14 (Appendix C accuracy) satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → Phase 5 CFG-01 | Incorrect field scope causes wrong reindex expectations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Tampering | Appendix C field claims | mitigate | Cross-check against libraryConfig.js REINDEX_FIELDS Set |
| T-02-06-SC | Tampering | n/a (doc-only) | accept | No package installs |
</threat_model>

<verification>
- Compare Appendix C rows against `libraryConfig.js` REINDEX_FIELDS — footnotes on all exceptions
- minParagraphLength trace: application.yml L158 → value 30
</verification>

<success_criteria>
- Appendix C scoped as「主矩阵相关字段子集」with non-REINDEX_FIELDS footnotes
- minParagraphLength anchor corrected to YAML + VectorLibraryConfig.java
- Verification gap #3 (Appendix C accuracy) closed
</success_criteria>

<output>
Create `.planning/phases/02-file-type-matrix/02-06-SUMMARY.md` when done
</output>

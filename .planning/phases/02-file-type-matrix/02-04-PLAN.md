---
phase: 02-file-type-matrix
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/docs/FILE-TYPE-PROCESSING.md
autonomous: true
gap_closure: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
must_haves:
  truths:
    - "§2 is a single contiguous 4-column matrix (设定|产出|质量) per D-03/D-04"
    - "PDF through Markdown matrix rows share one table header; dev-reference blocks do not interrupt the matrix"
    - "Excel/TXT/Markdown rows use 4 columns, not nested inside 3-column dev tables"
  artifacts:
    - path: ".planning/docs/FILE-TYPE-PROCESSING.md"
      provides: "§2 contiguous ops matrix"
      contains: "| **Excel** · `parsing.tableExtraction` |"
  key_links:
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md §2"
      to: "PDF/Word/Excel/TXT/Markdown row groups"
      via: "single markdown table"
      pattern: "^\| \*\*(PDF|Word|Excel|TXT|Markdown)\*\*"
---

<objective>
Restore §2 main matrix to D-03「单张大表」contract by closing the 4-column table after Word rows, relocating dev-reference subsections below the matrix, and ensuring Excel/TXT/Markdown rows sit in the same 4-column table as PDF/Word.

Purpose: Closes CR-01 / verification gap #1 — ops cannot read Excel→Markdown as one matrix while dev tables interrupt §2.
Output: `.planning/docs/FILE-TYPE-PROCESSING.md` §2 with structurally valid single matrix table.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-file-type-matrix/02-VERIFICATION.md
@.planning/phases/02-file-type-matrix/02-REVIEW.md
@.planning/phases/02-file-type-matrix/02-CONTEXT.md
@.planning/phases/02-file-type-matrix/02-03-SUMMARY.md
@.planning/docs/FILE-TYPE-PROCESSING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Close §2 matrix after Word rows and relocate dev-reference blocks</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - .planning/docs/FILE-TYPE-PROCESSING.md L81–160 — current broken §2 structure (matrix L85–104, dev blocks L106–127, Excel–MD rows L128–147 nested in 3-col table)
    - .planning/phases/02-file-type-matrix/02-VERIFICATION.md — gap CR-01 missing items
    - .planning/phases/02-file-type-matrix/02-REVIEW.md — CR-01 fix pattern
    - .planning/phases/02-file-type-matrix/02-CONTEXT.md — D-03, D-04
  </read_first>
  <action>
    Fix §2 table corruption per D-03/D-04 and verification gap CR-01:

    1. After the last Word matrix row (`| **Word** · \`IndexingChunkFilter\` | … |` at L104), **close** the main 4-column matrix table with a blank line — do NOT insert dev-reference subsections inside the table.

    2. **Move** both dev-reference blocks currently at L106–127 to **below** the complete main matrix (after all five type row groups):
       - `### 开发参考：PDF / Word 解析锚点 {#dev-reference-pdf-word}` — keep as 3-column table (路径 | 行为 | 矩阵关联)
       - `### 开发参考：Excel 解析与分块锚点 {#dev-reference-excel}` — keep as 3-column table only for code anchors (L120–127 content); **remove** Excel/TXT/Markdown matrix rows from inside this table

    3. **Re-insert** Excel/TXT/Markdown matrix rows (currently L128–147) as rows in the **same** 4-column main matrix table as PDF/Word — either:
       - Option A (preferred per D-03): keep one uninterrupted table — move dev blocks entirely below Markdown's last row; OR
       - Option B: close table after Word, append Excel–MD rows before dev blocks, then dev blocks below
       Choose Option A unless line count makes the table unwieldy; D-03 requires「单张大表」.

    4. Preserve all existing cell content for Excel/TXT/Markdown rows (ROADMAP 周报 xlsx anchor, MIME rows, 推荐/禁止 labels) — **structure only**, no content rewrites.

    5. Keep `### 开发参考：Resolver 消费链` (L149–158) below dev-reference subsections, outside any matrix table.

    Do NOT change anchor IDs in this plan (Plan 02-05 handles CR-02). Temporary `{#dev-reference}` on moved subsections is acceptable until Plan 05 renames them.
  </action>
  <acceptance_criteria>
    - Exactly one `| 类型 × 规则项 | 设定（推荐 config_json） | 产出形态 | 质量风险 / 禁止 |` header in §2
    - Dev-reference 3-column tables appear only after the main matrix table closes (no `### 开发参考` heading between PDF/Word rows and Excel rows)
    - Excel/TXT/Markdown rows have four pipe-delimited columns matching PDF/Word row format
    - All ROADMAP anchor rows (周报 xlsx, 扫描 pdf, 制度 docx) still present with 推荐/禁止
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$c=Get-Content '.planning/docs/FILE-TYPE-PROCESSING.md' -Raw; if(($c -split '\| 类型 × 规则项 \| 设定').Count -ne 2){exit 1}; $s2=$c -split '## §2 主矩阵' | Select-Object -Last 1; $s2=$s2 -split '## §3' | Select-Object -First 1; if($s2 -match '(?ms)\| \*\*Word\*\*.*?\n\n### 开发参考'){exit 1}; if($s2 -notmatch '\| \*\*Excel\*\* · `parsing\.tableExtraction` \|'){exit 1}; if($s2 -notmatch '\| \*\*Markdown\*\* · `IndexingChunkFilter` \|'){exit 1}"</automated>
  </verify>
  <done>§2 renders as one contiguous 4-column matrix; dev-reference blocks no longer interrupt type rows.</done>
</task>

<task type="auto">
  <name>Task 2: Validate matrix row coverage and markdown table integrity</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - .planning/docs/FILE-TYPE-PROCESSING.md — re-read §2 L81–170 after Task 1 edits
    - .planning/phases/02-file-type-matrix/02-02-SUMMARY.md — TYPE-01–04 row inventory
  </read_first>
  <action>
    Spot-check §2 after restructure:

    1. Count matrix rows per type: PDF ≥8, Word ≥7, Excel ≥8 (incl. 周报 sub-scene), TXT ≥5, Markdown ≥5 — match pre-fix content, not fewer.

    2. Confirm no matrix row line has fewer than 4 column cells (count `|` segments — type rows should have 5 pipe chars minimum per line for 4 columns).

    3. Confirm Excel D-08 target-state footnote block (L116 area) remains **outside** the main matrix table, between matrix end and dev-reference or as callout after Excel rows per D-01.

    4. Update §2 intro sentence if needed to reflect single-table layout (already states D-03 — no change unless contradicts structure).

    No anchor or cross-doc link edits in this task.
  </action>
  <acceptance_criteria>
    - All five file types represented in §2 main matrix
    - No 4-column matrix data rows inside 3-column dev-reference tables
    - §2 line count for matrix section ≥55 rows (content preserved)
  </acceptance_criteria>
  <verify>
    <automated>rg -v "^#" .planning/docs/FILE-TYPE-PROCESSING.md | rg -c "^\| \*\*(PDF|Word|Excel|TXT|Markdown)\*\*" | ForEach-Object { if ([int]$_ -lt 20) { exit 1 } }</automated>
  </verify>
  <done>CR-01 closed; verification truth #5 (single contiguous matrix) achievable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc-author → ops reader | Structural markdown errors mislead operational config choices |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | §2 matrix structure | mitigate | Automated table-structure verify in task verify blocks |
| T-02-04-SC | Tampering | n/a (doc-only) | accept | No package installs |
</threat_model>

<verification>
- Re-run VERIFICATION.md truth #5 probe: §2 L85–147 displays as one 4-column table without nested dev tables
- `rg "周报|扫描|制度" .planning/docs/FILE-TYPE-PROCESSING.md` still returns ROADMAP anchors with 推荐/禁止
</verification>

<success_criteria>
- §2 is a single contiguous 4-column matrix per D-03/D-04
- Dev-reference blocks sit below the closed matrix, not interrupting it
- Excel/TXT/Markdown rows no longer nested in 3-column dev table
</success_criteria>

<output>
Create `.planning/phases/02-file-type-matrix/02-04-SUMMARY.md` when done
</output>

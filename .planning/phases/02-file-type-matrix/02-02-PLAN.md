---
phase: 02-file-type-matrix
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .planning/docs/FILE-TYPE-PROCESSING.md
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
must_haves:
  truths:
    - "PDF rows document OCR on/off, table extraction, chunking strategy with scan-PDF 禁止 (TYPE-01, D-05)"
    - "Word rows document structured table extraction and heading-level for 制度 docx (TYPE-02)"
    - "Excel rows document text-only, paragraph-first, header filter, 周报 sub-scene with structured gap (TYPE-03, D-08–D-10)"
    - "TXT/Markdown rows document encoding detection and paragraph chunking (TYPE-04)"
    - "Appendix A MIME → config_json fragments table complete for Phase 3 preset reference (D-06, D-07)"
    - "ROADMAP anchors: 周报 xlsx, 扫描 pdf, 制度 docx each have explicit 推荐/禁止 in §2"
  artifacts:
    - path: ".planning/docs/FILE-TYPE-PROCESSING.md"
      provides: "§2 filled matrix rows and Appendix A MIME mapping"
      contains: "DocumentParseService"
  key_links:
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md"
      to: "DocumentParseService.java"
      via: "PDF OCR / Excel Tika path dev footnotes"
      pattern: "DocumentParseService|extractPlainWithTika|OcrFallbackPolicy"
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md"
      to: "TableExtractionMode.java"
      via: "Excel structured 禁止 row"
      pattern: "TableExtractionMode|structured"
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md"
      to: "IndexingChunkFilter.java"
      via: "Excel 表头过滤产出列"
      pattern: "IndexingChunkFilter|removeHeaderOnlyChunks"
---

<objective>
Fill §2 main matrix with per-type recommendation cells for all five file types (TYPE-01–04) and write Appendix A MIME → config_json mapping (D-06/D-07) plus Appendix B structured Excel gap (D-08–D-10).

Purpose: Delivers the core ops-facing 设定→产出→质量 content ROADMAP Phase 2 requires — especially 周报 xlsx, 扫描 pdf, 制度 docx 推荐/禁止 anchors.
Output: FILE-TYPE-PROCESSING.md §2 complete; 附录 A MIME table; 附录 B structured gap; all Plan 02 placeholders replaced.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-file-type-matrix/02-CONTEXT.md
@.planning/phases/02-file-type-matrix/02-PATTERNS.md
@.planning/codebase/CONCERNS.md
@.planning/docs/FILE-TYPE-PROCESSING.md
@.planning/docs/INGEST-PIPELINE.md
</context>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| `.planning/docs/FILE-TYPE-PROCESSING.md` §2, 附录 A/B | 02 | Filled type matrix + MIME appendix + Excel gap |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: PDF and Word matrix rows (TYPE-01, TYPE-02)</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - knowbase-service/src/main/java/com/knowbase/ingest/service/DocumentParseService.java — OCR fallback, HTML pipeline branch (lines ~44–93)
    - knowbase-service/src/main/java/com/knowbase/ingest/parse/DocumentParseOptions.java — requiresHtmlPipeline()
    - knowbase-service/src/main/java/com/knowbase/ingest/parse/OcrFallbackPolicy.java
    - knowbase-service/src/main/resources/application.yml — ingest.ocr tessdata, min-extracted-chars
    - .planning/phases/02-file-type-matrix/02-PATTERNS.md — PDF/Word matrix seed rows
  </read_first>
  <action>
    Replace §2 Plan 02 placeholders for **PDF** and **Word** row groups per TYPE-01/TYPE-02 and D-05:

    **PDF (TYPE-01)** — minimum filled rows:
    - 文本型 · parsing.ocrEnabled: `false` when Tika extracts ≥32 chars; 产出 Tika 纯文本 → parsed.txt; 低风险
    - 扫描件 · parsing.ocrEnabled: **`true`** + tessdata; 产出 OCR 文本; **禁止** OCR 关闭 → 0 chunk/乱码 (ROADMAP 扫描 pdf anchor)
    - parsing.tableExtraction: structured (HTML 管道) or text-only; note complex layout tab misalignment risk
    - chunkingStrategy: paragraph-first default; 制度/长 PDF optional heading-level
    - cleaning.removeHeaderFooter: true for exported PDFs
    - IndexingChunkFilter: N/A typical for scan PDF

    **Word (TYPE-02)** — minimum filled rows:
    - parsing.tableExtraction: **`structured`** triggers HTML pipeline; 产出 HtmlParsingContentProcessor 表格行; **禁止** complex tables with text-only only
    - chunkingStrategy: 制度 docx **`heading-level`**; short docs paragraph-first (ROADMAP 制度 docx anchor)
    - cleaning defaults: removeHeaderFooter true for 制度文档
    - IndexingChunkFilter: standard pipeline

    Each 设定 cell uses config_json dot-paths aligned with libraryConfig.js. 质量 column uses 推荐/禁止 vocabulary for ROADMAP scenarios.

    Add dev code-anchor footnotes: DocumentParseService OCR branch, DocumentParseOptions.requiresHtmlPipeline(), cite file paths.

    Do NOT write Excel/TXT rows in this task — Task 2/3.
  </action>
  <acceptance_criteria>
    - Scan PDF row has explicit **禁止** OCR-off language
    - 制度 docx row recommends heading-level + structured table extraction
    - All PDF/Word skeleton rows filled (no remaining Plan 02 for these types)
    - DocumentParseService cited ≥2 times in PDF/Word footnotes
  </acceptance_criteria>
  <verify>
    <automated>rg -c "扫描|ocrEnabled|structured|heading-level|制度|DocumentParseService" .planning/docs/FILE-TYPE-PROCESSING.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>TYPE-01 and TYPE-02 satisfied from §2 PDF/Word rows; ROADMAP 扫描 pdf and 制度 docx anchors present.</done>
</task>

<task type="auto">
  <name>Task 2: Excel matrix rows and Appendix B structured gap (TYPE-03)</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - knowbase-service/src/main/java/com/knowbase/ingest/service/DocumentParseService.java — extractPlainWithTika for xlsx (lines ~95–107)
    - knowbase-service/src/main/java/com/knowbase/ingest/parse/TableExtractionMode.java — structured semantics
    - knowbase-service/src/main/java/com/knowbase/vector/chunk/IndexingChunkFilter.java — removeHeaderOnlyChunks
    - knowbase-service/src/test/java/com/knowbase/vector/service/ChunkPreviewServiceTest.java — 杜鹏飞 fixture (optional footnote D-12)
    - .planning/codebase/CONCERNS.md — Excel flat Tika, structured HTML-only
    - .planning/phases/02-file-type-matrix/02-CONTEXT.md — D-08, D-09, D-10
  </read_first>
  <action>
    Replace §2 Plan 02 placeholders for **Excel** row group per TYPE-03:

    Required filled rows:
    - parsing.tableExtraction: **`text-only`** (v1 only effective); **禁止** `structured` on xlsx — no benefit, wrong expectation (link 附录 B)
    - chunkingStrategy: **`paragraph-first`**; **禁止** `semantic` — continuation row / header semantic mis-split
    - 续行/表头: cleaning default + TabularContinuationNormalizer in pipeline narrative; merged tab continuation paragraphs
    - IndexingChunkFilter: filters pure header blocks like「序号\t类别\t…」; note noise if headers dominate
    - **Excel · 子场景（周报 xlsx）**: same as above + homogeneous semantic single library; 3–4 chunks/doc at chunkSize=500; **禁止** mixing 周报+报销 same library (ROADMAP 周报 xlsx anchor)

    Target-state one-liner in Excel intro (D-08): structured ingest needed for accurate chunking — then transition recommendation (D-09): v1 ops use text-only + paragraph-first.

    Write 附录 B: 结构化 Excel 差距与 backlog per D-08–D-10:
    - Gap table: 目标态 structured row objects vs v1 Tika tab text → paragraph-first → IndexingChunkFilter
    - tableExtraction structured applies HTML pipeline only — Excel **不适用**
    - Explicit backlog links: PROJECT Out of Scope POI/dual-track, INGEST-PIPELINE 附录 B.3 QueryRouter, CONCERNS Tech Debt template
    - Do NOT imply v1 can deliver structured Excel ingest

    Optional D-12 footnote: ChunkPreviewServiceTest 杜鹏飞 params (chunkSize=500, overlap=120 → 3 chunks after filter) — not mandatory acceptance.

    Replace 附录 B placeholder from Plan 01 if present.
  </action>
  <acceptance_criteria>
    - Excel structured explicitly **禁止** with Appendix B cross-ref
    - 周报 xlsx sub-row has 推荐/禁止 for homogeneous library
    - Appendix B contains 目标态/v1/过渡推荐/Backlog columns
    - D-10 backlog references PROJECT Out of Scope — no POI implementation promise
    - IndexingChunkFilter and TabularContinuationNormalizer mentioned in 产出 column
  </acceptance_criteria>
  <verify>
    <automated>rg -c "text-only|paragraph-first|structured|附录 B|周报|IndexingChunkFilter|POI|Out of Scope" .planning/docs/FILE-TYPE-PROCESSING.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>TYPE-03 satisfied; Excel transition path and structured backlog explicit; ROADMAP 周报 xlsx anchor present.</done>
</task>

<task type="auto">
  <name>Task 3: TXT/Markdown rows and Appendix A MIME mapping (TYPE-04, D-06, D-07)</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - knowbase-service/src/main/java/com/knowbase/library/config/VectorLibraryConfigFactory.java — FILE_TYPE_MIMES map (lines ~14–23)
    - knowbase-service/src/main/java/com/knowbase/ingest/support/MimeTypeAllowlist.java
    - frontend/knowbase-ui/src/utils/supportedFileTypes.js — EXT_MAP
    - frontend/knowbase-ui/src/utils/libraryDefaults.js — parsing.autoDetectEncoding
    - .planning/phases/02-file-type-matrix/02-PATTERNS.md — Appendix A MIME template, D-07 tier narrative
  </read_first>
  <action>
    Replace §2 Plan 02 placeholders for **TXT** and **Markdown** row groups per TYPE-04:

    **TXT:**
    - parsing.autoDetectEncoding: **`true`**; 产出 correct UTF-8/GBK decode; 禁止 false on GBK → garbled chunks
    - chunkingStrategy: paragraph-first; chunkSize footnote for very short files
    - cleaning: defaults apply

    **Markdown:**
    - ingestAccess must include markdown in supportedFileTypes
    - MIME: text/markdown, text/x-markdown; .md may arrive as text/plain — MimeTypeAllowlist extension fallback
    - chunkingStrategy: paragraph-first or heading-level when `#` headers present; **禁止** semantic on code blocks

    Write 附录 A: MIME → 推荐 config_json 片段 per D-06:
    - Table columns: MIME | 文件类型键 | 扩展名 | 推荐 config_json 片段（库级初始默认）
    - Rows: application/pdf, word MIMEs, excel MIMEs, text/plain, markdown MIMEs — copy from VectorLibraryConfigFactory + supportedFileTypes.js
    - Banner: 「文档规划，Phase 3 预设引用」— not v1 runtime MIME auto-default engine (deferred)

    Write D-07 three-tier override narrative after table:
    1. Appendix A = library initial default (Phase 3 libraryPresets.js source)
    2. Vertical libraries may override (e.g., scan library global ocrEnabled: true)
    3. Ingest-level override = target backlog (INGEST-PIPELINE 附录 B.3)

    Update §3 类型推荐值 column for rows affected by Appendix A (brief sync).

    Remove all remaining §2 `（Plan 02 填充）` placeholders.
  </action>
  <acceptance_criteria>
    - TXT and Markdown rows complete with autoDetectEncoding and chunking guidance
    - Appendix A has ≥5 MIME rows with config_json fragments
    - Phase 3 preset reference banner present; runtime engine deferred
    - D-07 three override tiers documented
    - No Plan 02 placeholders remain in §2
  </acceptance_criteria>
  <verify>
    <automated>rg -c "autoDetectEncoding|text/markdown|附录 A|Phase 3|MIME|Plan 02" .planning/docs/FILE-TYPE-PROCESSING.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>TYPE-04 satisfied; Appendix A complete for Phase 3; all §2 matrix cells filled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc → ops config | Wrong Excel structured recommendation causes zero-benefit misconfiguration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Excel structured 禁止 row | mitigate | Verify against DocumentParseService extractPlainWithTika path and TableExtractionMode source |
| T-02-04 | Repudiation | Appendix A MIME map | mitigate | Cross-check MIME list against VectorLibraryConfigFactory.FILE_TYPE_MIMES |
</threat_model>

<verification>
- ROADMAP three anchors grep-able: 周报 xlsx, 扫描 pdf, 制度 docx with 推荐/禁止
- TYPE-01–04 traceable from §2 rows alone
- Appendix B does not promise POI/structured v1 delivery
</verification>

<success_criteria>
- TYPE-01 through TYPE-04 requirements satisfied from §2 + 附录 A/B
- D-05, D-06, D-07, D-08, D-09, D-10 implemented in matrix and appendices
- All §2 cells filled; no Plan 02 placeholders
</success_criteria>

<output>
Create `.planning/phases/02-file-type-matrix/02-02-SUMMARY.md` when done
</output>

---
phase: 02-file-type-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/docs/FILE-TYPE-PROCESSING.md
autonomous: true
requirements:
  - TYPE-01
  - TYPE-02
  - TYPE-03
  - TYPE-04
must_haves:
  truths:
    - "Reader can find separate ops and dev entry points in FILE-TYPE-PROCESSING.md (D-13)"
    - "Document states target-state-first narrative with Excel structured gap deferred to appendix (D-01, D-02)"
    - "Three-tier defaults table maps system/wizard/type-recommend values with diff notes (D-14)"
    - "Main matrix skeleton exists with 设定|产出|质量 columns and full pipeline row keys (D-03, D-04)"
    - "TYPE-01–04 traceability table present with section placeholders"
  artifacts:
    - path: ".planning/docs/FILE-TYPE-PROCESSING.md"
      provides: "Phase 2 doc scaffold through §3 defaults and §2 matrix skeleton"
      min_lines: 150
      contains: "TYPE-01"
  key_links:
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md"
      to: ".planning/docs/INGEST-PIPELINE.md"
      via: "§1 upstream cross-ref — no PIPE flow duplication"
      pattern: "INGEST-PIPELINE|§7|§8"
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md"
      to: "application.yml"
      via: "§3 系统默认 column"
      pattern: "application\\.yml|chunking\\.strategy"
    - from: ".planning/docs/FILE-TYPE-PROCESSING.md"
      to: "libraryDefaults.js"
      via: "§3 向导默认 column"
      pattern: "libraryDefaults|defaultLibraryConfig"
---

<objective>
Create FILE-TYPE-PROCESSING.md foundation: dual-audience reader guide, target-state boundary, three-tier defaults table (D-14), and §2 main matrix skeleton with full pipeline row keys per D-03/D-04.

Purpose: Establishes the single-table structure and config tier baseline ops/dev must share before per-type row content (Plan 02) and anti-patterns (Plan 03).
Output: `.planning/docs/FILE-TYPE-PROCESSING.md` §1, §3, §2 skeleton with placeholder rows for all five types; no filled per-type recommendations yet (Plan 02).
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-file-type-matrix/02-CONTEXT.md
@.planning/phases/02-file-type-matrix/02-PATTERNS.md
@.planning/docs/INGEST-PIPELINE.md
@.planning/codebase/CONCERNS.md
</context>

<artifacts_this_phase_produces>
| Artifact | Plan | Description |
|----------|------|-------------|
| `.planning/docs/FILE-TYPE-PROCESSING.md` | 01–03 | Primary deliverable — per-type processing matrix |
| `.planning/STATE.md` | 03 | Phase 2 completion status update |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold doc with dual TOC and PIPE cross-refs</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - .planning/phases/02-file-type-matrix/02-PATTERNS.md — doc-root front matter, dual TOC, upstream cross-ref pattern
    - .planning/phases/02-file-type-matrix/02-CONTEXT.md — D-01, D-02, D-13 locked decisions
    - .planning/docs/INGEST-PIPELINE.md — §1 structure, D-17 Phase 2 deferral pointer to reverse-link
  </read_first>
  <action>
    Create `.planning/docs/FILE-TYPE-PROCESSING.md` with YAML front matter (`last_mapped_commit`, `analysis_date: 2026-06-10`, `focus: file-type-processing`).

    Write title `# 按文件类型处理设定矩阵` and Phase 2 doc-only banner per PATTERNS (target-state-first D-01; Excel structured gap in appendix B, not dominant in body).

    Write ## 目录 dual TOC per D-13:
    - 运营 → `#ops-guide` → §2 主矩阵（推荐/禁止/质量风险）、§4 类型反模式
    - 开发 → `#dev-reference` → §3 三层默认值、附录 A MIME 映射、附录 C 字段路径

    Write §1 范围与读者指南 with both `{#ops-guide}` and `{#dev-reference}` anchors containing:
    - Milestone core value quote from PROJECT.md
    - 「与全链路文档关系」 subsection per PATTERNS: link INGEST-PIPELINE §2–§4 (流程/API), §7 (质量), §8 (通用反模式); explicit statement this doc does NOT duplicate PIPE flow
    - Phase boundary: pdf/word/excel/txt/markdown; MIME runtime engine = Phase 3; structured Excel ingest = backlog (D-02)
    - TYPE-01–05 traceability table with section placeholders (§2 PDF/Word/Excel/TXT-MD, §4 TYPE-05)
    - ROADMAP success criteria anchor paragraph: 周报 xlsx、扫描 pdf、制度 docx must each have 推荐/禁止 in §2 (filled Plan 02)

    Add placeholder headings: §2 主矩阵, §3 三层默认值对照, §4 类型反模式, 附录 A MIME, 附录 B 结构化 Excel 差距, 附录 C 字段路径, §9 验收清单 — mark §4/附录/§9 as "（Plan 03 填充）" one-liners; §2 body as skeleton only (Task 3).

    Do NOT write per-type recommendation cell values yet — skeleton rows only.
  </action>
  <acceptance_criteria>
    - File exists with front matter and both `#ops-guide` / `#dev-reference` anchors
    - D-01 target-state-first and Phase 2 doc-only boundary stated
    - INGEST-PIPELINE.md linked ≥3 times (§2–§4, §7, §8)
    - TYPE-01–05 traceability table present
    - No duplicate ingest pipeline stage narrative
  </acceptance_criteria>
  <verify>
    <automated>rg -c "#ops-guide|#dev-reference|INGEST-PIPELINE|TYPE-01|TYPE-05" .planning/docs/FILE-TYPE-PROCESSING.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Scaffold exists; ops and dev can navigate; upstream PIPE cross-refs in place without flow duplication.</done>
</task>

<task type="auto">
  <name>Task 2: Three-tier defaults table (D-14)</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - knowbase-service/src/main/resources/application.yml — ingest.ocr, chunking.* system defaults (lines ~59–67, ~152–160)
    - frontend/knowbase-ui/src/utils/libraryDefaults.js — defaultLibraryConfig parsing/cleaning/chunking blocks
    - knowbase-service/src/main/java/com/knowbase/library/config/ParsingRulesSettings.java — field names and defaults
    - knowbase-service/src/main/java/com/knowbase/library/config/VectorLibraryConfigFactory.java — system chunking fallback 600/100
    - .planning/phases/02-file-type-matrix/02-PATTERNS.md — §3 三层默认值对照 template
  </read_first>
  <action>
    Write §3 三层默认值对照 per D-14 with table columns:
    规则项 | 配置路径 | 系统默认 (application.yml) | 向导默认 (libraryDefaults.js) | 类型推荐值 (汇总) | 一致/差异说明

    Minimum rows (verify against source):
    - chunkingStrategy → paragraph-first (yml L153) vs paragraph-first (libraryDefaults L39) — note Word long-doc exception in 类型推荐 column as "heading-level（制度 docx，见 §2）"
    - chunkSize → 600 (yml) vs 500 (wizard) — **差异** flagged explicitly
    - chunkOverlap → 100 vs 120 — **差异**
    - parsing.ocrEnabled → — (library-level) vs false — note ingest.ocr.enabled is engine switch only, not library default
    - parsing.tableExtraction → — vs text-only — note Word structured / Excel text-only in 类型推荐
    - parsing.autoDetectEncoding → — vs true — TXT/MD recommend true
    - cleaning.removeHeaderFooter → — vs true
    - cleaning.removeDuplicateParagraphs → — vs true

    Add short narrative: system tier = platform ceiling/fallback; wizard tier = CreateLibraryWizard initial values; type recommend = §2 matrix 设定 column (Plan 02 fills per-type cells).

    Cite code anchors: application.yml keys, libraryDefaults.js line refs, ParsingRulesSettings.java field names.

    Cross-ref INGEST-PIPELINE §5 for tier model consistency — do not duplicate full four-tier ingest matrix.
  </action>
  <acceptance_criteria>
    - ≥8 rule rows with all five table columns populated
    - chunkSize 600 vs 500 and chunkOverlap 100 vs 120 marked as 差异
    - ingest.ocr.enabled distinguished from parsing.ocrEnabled
    - ParsingRulesSettings and libraryDefaults.js cited
  </acceptance_criteria>
  <verify>
    <automated>rg -c "application\\.yml|libraryDefaults|chunkSize|chunkOverlap|parsing\\.ocrEnabled|差异" .planning/docs/FILE-TYPE-PROCESSING.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Dev/ops can compare system vs wizard vs type-recommend defaults in one table; known diffs explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Main matrix skeleton with pipeline row keys</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - .planning/phases/02-file-type-matrix/02-CONTEXT.md — D-03, D-04, D-05
    - .planning/phases/02-file-type-matrix/02-PATTERNS.md — §2 主矩阵 row key naming
    - .planning/docs/INGEST-PIPELINE.md — §5.1 rule item naming alignment
    - frontend/knowbase-ui/src/utils/libraryConfig.js — REINDEX_FIELDS dot-paths
  </read_first>
  <action>
    Write §2 主矩阵 per D-03/D-04 as **single large table** with columns:
    类型 × 规则项 | 设定（推荐 config_json） | 产出形态 | 质量风险 / 禁止

    Create skeleton rows for all five types (pdf, word, excel, txt, markdown) covering full pipeline dimensions per D-04:
    - parsing.ocrEnabled (PDF only row; others N/A or —)
    - parsing.tableExtraction
    - parsing.autoDetectEncoding (TXT/Markdown)
    - parsing.defaultLanguage (footnote OCR)
    - cleaning.removeHeaderFooter, cleaning.removeDuplicateParagraphs
    - chunkingStrategy, chunkSize, chunkOverlap, minParagraphLength
    - IndexingChunkFilter (标注「非 config_json，入库启发式」)

    For each skeleton row: populate 类型 × 规则项 column with bold type prefix (**PDF** · …); leave 设定/产出/质量 cells as `（Plan 02 填充）` except column header row.

    Add matrix intro paragraph per D-03: one table for horizontal comparison; Excel 子场景（周报 xlsx）as indented sub-row placeholder under Excel group.

    Add dev footnote block listing resolver chain: parseOptionsFor → DocumentParseService; chunkingFor → IndexingService → IndexingChunkFilter — cite class paths without duplicating INGEST-PIPELINE §3.

    Do NOT fill recommendation values — Plan 02 replaces placeholders.
  </action>
  <acceptance_criteria>
    - Single §2 table (not five separate type chapters) per D-03
    - All five types represented with ≥8 pipeline row keys each (shared rows may merge with type-specific notes)
    - Columns 设定|产出|质量 present
    - IndexingChunkFilter row marked non-config_json
    - Excel 周报 xlsx sub-scenario row placeholder exists
  </acceptance_criteria>
  <verify>
    <automated>rg -c "PDF|Word|Excel|TXT|Markdown|parsing\\.ocrEnabled|IndexingChunkFilter|chunkingStrategy|Plan 02" .planning/docs/FILE-TYPE-PROCESSING.md | rg -v "^#" | wc -l</automated>
  </verify>
  <done>Matrix structure locked; Plan 02 executor fills cell values without restructuring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Doc author → codebase | Default values must match application.yml and libraryDefaults.js; wrong defaults mislead ops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | §3 defaults table | mitigate | Cross-check chunkSize/chunkOverlap against application.yml and libraryDefaults.js before merge |
| T-02-02 | Information Disclosure | Doc cites test fixtures | accept | No production PII in scaffold; fixture refs deferred to Plan 03 footnotes |
</threat_model>

<verification>
- §3 default values verifiable against application.yml and libraryDefaults.js
- §2 is single table with D-03 column structure
- INGEST-PIPELINE cross-refs present; no PIPE stage duplication
</verification>

<success_criteria>
- D-01, D-03, D-04, D-13, D-14 represented in §1–§3
- Document ≥150 lines with §2 skeleton ready for Plan 02 fill
- TYPE-01–04 traceability placeholders present
</success_criteria>

<output>
Create `.planning/phases/02-file-type-matrix/02-01-SUMMARY.md` when done
</output>

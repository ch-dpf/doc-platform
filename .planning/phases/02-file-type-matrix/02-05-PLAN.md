---
phase: 02-file-type-matrix
plan: 05
type: execute
wave: 2
depends_on:
  - 02-04
files_modified:
  - .planning/docs/FILE-TYPE-PROCESSING.md
  - .planning/docs/INGEST-PIPELINE.md
autonomous: true
gap_closure: true
requirements:
  - TYPE-05
must_haves:
  truths:
    - "Ops/dev anchors resolve uniquely — TOC jumps to §2 matrix vs §4 anti-patterns vs §3 defaults"
    - "INGEST-PIPELINE §7.5/§8 backlinks target FILE-TYPE-PROCESSING §2 and §4 distinctly (D-13)"
    - "§4 remediation links and §9 checklist steps land on intended sections"
  artifacts:
    - path: ".planning/docs/FILE-TYPE-PROCESSING.md"
      provides: "Unique section anchors"
      contains: "{#ops-matrix}"
    - path: ".planning/docs/INGEST-PIPELINE.md"
      provides: "Correct fragment backlinks"
      contains: "#ops-matrix"
  key_links:
    - from: ".planning/docs/INGEST-PIPELINE.md §7.5"
      to: ".planning/docs/FILE-TYPE-PROCESSING.md#ops-matrix"
      via: "markdown link fragment"
      pattern: "FILE-TYPE-PROCESSING\.md#ops-matrix"
    - from: ".planning/docs/INGEST-PIPELINE.md §8"
      to: ".planning/docs/FILE-TYPE-PROCESSING.md#ops-anti-patterns"
      via: "markdown link fragment"
      pattern: "FILE-TYPE-PROCESSING\.md#ops-anti-patterns"
---

<objective>
Eliminate duplicate `#ops-guide` / `#dev-reference` anchor IDs and wire bidirectional cross-doc links so ops/dev TOC entries and INGEST-PIPELINE backlinks land on §2 matrix, §4 anti-patterns, and §3 defaults — not always §1.

Purpose: Closes CR-02 / verification gaps #2 and #13 — dual-audience navigation per D-13 with unique section anchors.
Output: Updated anchor map in both `.planning/docs/FILE-TYPE-PROCESSING.md` and `.planning/docs/INGEST-PIPELINE.md`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-file-type-matrix/02-VERIFICATION.md
@.planning/phases/02-file-type-matrix/02-REVIEW.md
@.planning/phases/02-file-type-matrix/02-CONTEXT.md
@.planning/phases/02-file-type-matrix/02-04-SUMMARY.md
@.planning/docs/FILE-TYPE-PROCESSING.md
@.planning/docs/INGEST-PIPELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assign unique anchors and update FILE-TYPE-PROCESSING internal links</name>
  <files>.planning/docs/FILE-TYPE-PROCESSING.md</files>
  <read_first>
    - .planning/docs/FILE-TYPE-PROCESSING.md — all `{#ops-guide}` and `{#dev-reference}` occurrences (L22, L81, L106, L118, L162, L193, L227, L257, L278, L314)
    - .planning/phases/02-file-type-matrix/02-REVIEW.md — CR-02 anchor mapping
    - .planning/phases/02-file-type-matrix/02-CONTEXT.md — D-13 dual TOC intent
    - .planning/phases/02-file-type-matrix/02-VERIFICATION.md — gap missing items
  </read_first>
  <action>
    Replace duplicate anchors per D-13 (unique IDs per section, preserving dual-audience TOC):

    **Anchor assignment map (exact IDs):**
    | Section | New ops anchor | New dev anchor |
    |---------|----------------|----------------|
    | §1 范围与读者指南 | `{#ops-guide}` (landing only — **single** occurrence) | `{#dev-reference}` (landing only — **single** occurrence) |
    | §2 主矩阵 | `{#ops-matrix}` | — |
    | §3 三层默认值 | — | `{#dev-defaults}` |
    | §4 类型反模式 | `{#ops-anti-patterns}` | — |
    | 附录 A | — | `{#dev-appendix-a}` |
    | 附录 B | `{#ops-appendix-b}` | — |
    | 附录 C | — | `{#dev-field-paths}` |
    | §9 验收清单 | `{#ops-checklist}` | `{#dev-checklist}` |
    | Dev sub §2 PDF/Word | — | `{#dev-reference-pdf-word}` (from Plan 04) |
    | Dev sub §2 Excel | — | `{#dev-reference-excel}` (from Plan 04) |

    1. Remove duplicate `{#ops-guide}` from §2, §4, §9, appendix B — assign per map above.
    2. Remove duplicate `{#dev-reference}` from §3, dev subsections, appendices A/C, §9 — assign per map.
    3. Update **目录** table (L15–18):
       - 运营 → `#ops-matrix` (§2) and `#ops-anti-patterns` (§4); note §9 at `#ops-checklist`
       - 开发 → `#dev-defaults` (§3), `#dev-appendix-a`, `#dev-field-paths` (附录 C)

    4. Update **§4** intro and every「正确做法」link: replace `#ops-guide` with `#ops-matrix` for §2 row references.

    5. Update **§9** checklist steps 1–6: step 1 → `#ops-matrix`; step 2 → `#dev-defaults`; step 4 → `#ops-anti-patterns`; step 6 → verify INGEST-PIPELINE links (text only — Task 2 edits PIPE file).

    6. Grep entire file for remaining `#ops-guide` / `#dev-reference` link targets that should point to section-specific anchors; fix all internal cross-refs.

    Do NOT edit Appendix C table content (Plan 02-06 handles WR-01/WR-02).
  </action>
  <acceptance_criteria>
    - `{#ops-guide}` appears at most once (§1 landing)
    - `{#dev-reference}` appears at most once (§1 landing)
    - §2 heading includes `{#ops-matrix}`; §4 includes `{#ops-anti-patterns}`; §3 includes `{#dev-defaults}`; 附录 C includes `{#dev-field-paths}`
    - §4 table has zero `#ops-guide` fragment links (all `#ops-matrix` or `#ops-anti-patterns`)
    - §9 step 4 link targets `#ops-anti-patterns`, not `#ops-guide`
  </acceptance_criteria>
  <verify>
    <automated>powershell -Command "$c=Get-Content '.planning/docs/FILE-TYPE-PROCESSING.md' -Raw; if(([regex]::Matches($c,'\{#ops-guide\}')).Count -gt 1){exit 1}; if(([regex]::Matches($c,'\{#dev-reference\}')).Count -gt 1){exit 1}; if($c -notmatch '\{#ops-matrix\}'){exit 1}; if($c -notmatch '\{#ops-anti-patterns\}'){exit 1}; if($c -notmatch '\{#dev-defaults\}'){exit 1}; if($c -notmatch '\{#dev-field-paths\}'){exit 1}"</automated>
  </verify>
  <done>Intra-doc navigation resolves §2 vs §4 vs §3 distinctly.</done>
</task>

<task type="auto">
  <name>Task 2: Update INGEST-PIPELINE backlinks to new anchors</name>
  <files>.planning/docs/INGEST-PIPELINE.md</files>
  <read_first>
    - .planning/docs/INGEST-PIPELINE.md L51, L470, L472, L488, L490 — current broken `#ops-guide` links
    - .planning/phases/02-file-type-matrix/02-VERIFICATION.md — key link table (INGEST-PIPELINE → FILE-TYPE)
    - .planning/phases/02-file-type-matrix/02-REVIEW.md — WR-04 fix
    - .planning/docs/FILE-TYPE-PROCESSING.md — confirm new anchor IDs from Task 1
  </read_first>
  <action>
    Update INGEST-PIPELINE.md FILE-TYPE-PROCESSING backlinks per CR-02 / WR-04:

    1. **L51** (§1 Phase 2 deferral): split links —
       `[§2 主矩阵](./FILE-TYPE-PROCESSING.md#ops-matrix)` and `[§4 类型反模式](./FILE-TYPE-PROCESSING.md#ops-anti-patterns)`

    2. **§7.5** (~L470): §2 recommendation link → `#ops-matrix`; §4 anti-patterns link → `#ops-anti-patterns` (replace both `#ops-guide` uses)

    3. **§8** (~L488, L490): PDF scan row → `#ops-matrix` for §2 PDF row + `#ops-anti-patterns` for §4; Excel structured row → `#ops-matrix` for §2 Excel + `#ops-anti-patterns` for §4; keep `#appendix-b` link unchanged

    4. Grep entire INGEST-PIPELINE.md for `FILE-TYPE-PROCESSING.md#ops-guide` — replace each with context-appropriate `#ops-matrix` or `#ops-anti-patterns`

    5. Update FILE-TYPE-PROCESSING §9 step 6 / §9 双向交叉引用检查 table if it lists expected PIPE fragments — ensure documented targets match Task 2 edits.

    Minimal edit scope: link fragments only; do not restructure Phase 1 PIPE sections.
  </action>
  <acceptance_criteria>
    - Zero `FILE-TYPE-PROCESSING.md#ops-guide` links in INGEST-PIPELINE.md (unless intentionally pointing to §1 landing — prefer `#ops-matrix` for §2 content)
    - At least one `#ops-matrix` and one `#ops-anti-patterns` link in INGEST-PIPELINE.md
    - §7.5 and §8 Phase 2 cross-refs distinguish §2 vs §4
  </acceptance_criteria>
  <verify>
    <automated>rg "FILE-TYPE-PROCESSING\.md#ops-guide" .planning/docs/INGEST-PIPELINE.md; if ($LASTEXITCODE -eq 0) { exit 1 }; rg -c "FILE-TYPE-PROCESSING\.md#ops-matrix" .planning/docs/INGEST-PIPELINE.md | ForEach-Object { if ([int]$_ -lt 1) { exit 1 } }; rg -c "FILE-TYPE-PROCESSING\.md#ops-anti-patterns" .planning/docs/INGEST-PIPELINE.md | ForEach-Object { if ([int]$_ -lt 1) { exit 1 } }</automated>
  </verify>
  <done>CR-02 closed; verification truths #2 and #13 satisfied for cross-doc navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc cross-ref → reader | Broken anchors hide anti-pattern guidance from PIPE readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | anchor IDs | mitigate | Regex verify: each anchor ID appears ≤1 time |
| T-02-05-SC | Tampering | n/a (doc-only) | accept | No package installs |
</threat_model>

<verification>
- Click-test equivalent: `rg "FILE-TYPE-PROCESSING.md#"` both docs — no duplicate fragment targets for §2/§4 content
- §9 step 1 → `#ops-matrix`, step 4 → `#ops-anti-patterns` in FILE-TYPE-PROCESSING.md
</verification>

<success_criteria>
- Unique anchors per section; dual TOC at doc top resolves correctly
- INGEST-PIPELINE §7.5/§8 backlinks target §2 and §4 distinctly
- §4 remediation and §9 checklist links land on intended sections
</success_criteria>

<output>
Create `.planning/phases/02-file-type-matrix/02-05-SUMMARY.md` when done
</output>

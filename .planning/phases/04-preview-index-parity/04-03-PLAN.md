---
phase: 04-preview-index-parity
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - frontend/knowbase-ui/src/views/IngestView.vue
  - frontend/knowbase-ui/src/components/CreateLibraryWizard.vue
  - frontend/knowbase-ui/src/utils/chunkPreviewSample.js
  - frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue
autonomous: true
requirements:
  - PARITY-01
  - PARITY-03
must_haves:
  truths:
    - "IngestView chunk preview uses libraryChunkParams only — no overrideChunkSize affecting preview vs index"
    - "CreateLibraryWizard step-3 preview uses libraryChunkParams instead of resolvePreviewChunkParams scaling"
    - "After saving library settings (e.g. tableExtraction), IngestView reloads libraryConfig and next preview reflects new parsing/chunking"
  artifacts:
    - path: "frontend/knowbase-ui/src/views/IngestView.vue"
      provides: "buildChunkPreviewBody without override; config refresh on saved"
    - path: "frontend/knowbase-ui/src/components/CreateLibraryWizard.vue"
      provides: "libraryChunkParams for wizard chunk preview"
  key_links:
    - from: "IngestView.vue"
      to: "POST /api/v1/index/chunk-preview"
      via: "buildChunkPreviewBody sends libraryId + libraryChunkParams"
      pattern: "libraryChunkParams|libraryId"
    - from: "EditLibrarySettingsDrawer.vue"
      to: "IngestView.vue"
      via: "emit saved → parent reloads library"
      pattern: "emit\\('saved'|loadLibrary"
---

<objective>
Align frontend preview with backend parity rules (PARITY-01, PARITY-03): remove misleading overrideChunk UI, unify on libraryChunkParams, refresh config after save.

Purpose: Closes INGEST-PIPELINE §4.4 UI gaps — overrideChunk only preview, misleading «仅本次预览与入库» copy, wizard resolvePreviewChunkParams temp scaling.
Output: IngestView + CreateLibraryWizard preview bodies match library config; config hot-reload after edit.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-preview-index-parity/04-02-PLAN.md
@.planning/docs/INGEST-PIPELINE.md
@frontend/knowbase-ui/src/utils/chunkPreviewSample.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: IngestView — remove overrideChunk divergence + config reload</name>
  <files>frontend/knowbase-ui/src/views/IngestView.vue</files>
  <read_first>
    - frontend/knowbase-ui/src/views/IngestView.vue — buildChunkPreviewBody (~1128), overrideChunk UI (~516–531), libraryConfig load (~931), INGEST_CHUNK_PREVIEW_HINT usage
    - frontend/knowbase-ui/src/utils/chunkPreviewSample.js — libraryChunkParams, INGEST_CHUNK_PREVIEW_HINT
    - frontend/knowbase-ui/src/api/chunk.js — previewChunks payload shape
  </read_first>
  <action>
    Remove overrideChunk feature entirely (PARITY-01): delete overrideChunkEnabled, overrideChunkSize refs, el-form-item «覆盖块大小», and conditional chunkSize override in buildChunkPreviewBody. Preview always uses libraryChunkParams(cfg) for all sizing fields.

    buildChunkPreviewBody: always include libraryId; send chunkingStrategy, chunkSize, chunkOverlap, minChunkSize, maxChunkSize, minParagraphLength, normalizeBeforeChunk, textNormalizationEnabled, textNormalization, cleaning from libraryConfig — backend Plan 04-02 ignores sizing when libraryId set but keep payload complete for null-libraryId edge cases.

    Display INGEST_CHUNK_PREVIEW_HINT near preview stats (replace any «仅本次预览与入库» remnants).

    On library settings saved (listen to EditLibrarySettingsDrawer @saved or reload when returning from settings tab): re-fetch getVectorLibrary(libraryId) and update libraryConfig ref so tableExtraction/parsing changes reflect on next runIngestPreview without page refresh (PARITY-03).

    When lockPipeline, remove override UI branch — show «分块参数与库配置一致» tip only.
  </action>
  <acceptance_criteria>
    - No overrideChunkEnabled/overrideChunkSize in IngestView.vue
    - buildChunkPreviewBody always uses libraryChunkParams without conditional chunkSize override
    - libraryConfig refreshes after settings save event
    - Misleading «仅本次预览与入库» string absent
  </acceptance_criteria>
  <verify>
    <automated>rg -v "^\\s*//" frontend/knowbase-ui/src/views/IngestView.vue | rg -c "overrideChunk" ; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>采集页预览与库配置绑定；无预览专用 chunk 覆盖。</done>
</task>

<task type="auto">
  <name>Task 2: CreateLibraryWizard — libraryChunkParams + saved emit hook</name>
  <files>frontend/knowbase-ui/src/components/CreateLibraryWizard.vue, frontend/knowbase-ui/src/utils/chunkPreviewSample.js, frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue</files>
  <read_first>
    - frontend/knowbase-ui/src/components/CreateLibraryWizard.vue — resolvePreviewChunkParams usage (~593), chunk preview fetch
    - frontend/knowbase-ui/src/components/EditLibrarySettingsDrawer.vue — submit/saved emit (~637)
    - frontend/knowbase-ui/src/utils/chunkPreviewSample.js — resolvePreviewChunkParams vs libraryChunkParams
  </read_first>
  <action>
    CreateLibraryWizard: replace resolvePreviewChunkParams with libraryChunkParams(form.config) for wizard chunk-preview body (libraryId null — backend uses request params). Remove previewHint UI that advertises «预览临时块大小» when adjusted=false path only; show CHUNK_STRATEGY_PREVIEW_HINTS[strategy] instead.

    chunkPreviewSample.js: mark resolvePreviewChunkParams @deprecated in JSDoc; keep export for backward compat but wizard must not import it. Ensure libraryChunkParams defaults match libraryDefaults.js (chunkSize 600 vs 500 — align to libraryDefaults authoritative values).

    EditLibrarySettingsDrawer: ensure emit('saved', data.library) includes updated config; no functional change if already correct — verify parent IngestView handles reload.

    Manual smoke: change tableExtraction in drawer → save → IngestView preview without reload — parsing output should differ on re-preview (PARITY-03 acceptance).
  </action>
  <acceptance_criteria>
    - CreateLibraryWizard does not import resolvePreviewChunkParams
    - Wizard preview uses libraryChunkParams exclusively
    - libraryChunkParams defaults consistent with libraryDefaults.js chunkSize/overlap
  </acceptance_criteria>
  <verify>
    <automated>rg "resolvePreviewChunkParams" frontend/knowbase-ui/src/components/CreateLibraryWizard.vue ; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }</automated>
  </verify>
  <done>向导分块预览与库字段一致，无临时缩放。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI → chunk-preview API | User-controlled preview trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | Stale libraryConfig in IngestView | mitigate | Reload library on settings saved event |
| T-04-SC | Tampering | npm installs | accept | No new packages |
</threat_model>

<verification>
- Grep: no overrideChunk in IngestView.vue
- Grep: CreateLibraryWizard.vue does not reference resolvePreviewChunkParams
- npm run build in frontend/knowbase-ui succeeds
</verification>

<success_criteria>
- Operator changing tableExtraction in edit drawer sees updated parse/chunk preview on next preview run without full page reload
- No UI path advertises preview-only chunk overrides that index ignores
</success_criteria>

<output>
Create `.planning/phases/04-preview-index-parity/04-03-SUMMARY.md` when done
</output>

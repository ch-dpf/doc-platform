---
phase: 04-preview-index-parity
plan: 02
type: tdd
wave: 2
depends_on:
  - 04-01
files_modified:
  - knowbase-service/src/test/java/com/knowbase/vector/chunk/LibraryChunkPipelineParityTest.java
  - knowbase-service/src/main/java/com/knowbase/vector/service/ChunkPreviewService.java
  - knowbase-service/src/main/java/com/knowbase/vector/dto/ChunkPreviewRequest.java
autonomous: true
requirements:
  - PARITY-02
  - PARITY-04
must_haves:
  truths:
    - "Automated test proves preview chunk() and chunkIndexedText() return identical counts and content for 杜鹏飞 weekly-report sample"
    - "When libraryId is provided, chunk preview ignores client-supplied chunkSize overrides and uses LibraryConfigResolver.chunkingFor"
  artifacts:
    - path: "knowbase-service/src/test/java/com/knowbase/vector/chunk/LibraryChunkPipelineParityTest.java"
      provides: "Preview vs index parity regression test"
      contains: "previewMatchesIndexPathForWeeklyReportSample"
  key_links:
    - from: "LibraryChunkPipelineParityTest.java"
      to: "LibraryChunkPipeline.java"
      via: "chunk vs chunkIndexedText on same processed text"
      pattern: "chunkIndexedText|chunk\\("
---

<objective>
Add parity regression tests (PARITY-04) and enforce libraryId-based chunk parameter resolution so preview cannot drift from index config (PARITY-02).

Purpose: Makes «预览 8 块入库 3 块» class regressions CI-detectable; closes client-body override gap (INGEST-PIPELINE §4.4).
Output: `LibraryChunkPipelineParityTest` + ChunkPreviewService libraryId resolution.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-preview-index-parity/04-01-PLAN.md
@.planning/docs/INGEST-PIPELINE.md
@knowbase-service/src/test/java/com/knowbase/vector/service/ChunkPreviewServiceTest.java
</context>

<feature>
  <name>Preview vs index chunk parity</name>
  <files>LibraryChunkPipelineParityTest.java, ChunkPreviewService.java</files>
  <behavior>
    Given 杜鹏飞 weekly-report plain text and library chunking config (paragraph-first, chunkSize=500, chunkOverlap=120, minParagraphLength=30):
    - chunk(libraryId, text) after normalize+clean produces processedText T
    - chunkIndexedText(libraryId, T) yields same chunks list, rawTotalChunks, filteredOutCount as chunk(libraryId, text)
    - Mismatching client chunkSize in ChunkPreviewRequest when libraryId set does NOT alter result
    Cases: weekly-report fixture from ChunkPreviewServiceTest; assert raw=4, filtered=1, total=3
  </behavior>
  <implementation>
    RED: Add LibraryChunkPipelineParityTest with @SpringBootTest or lightweight unit test wiring real ChunkingService + filter (mock resolver returning fixed ChunkingProperties).
    GREEN: Update ChunkPreviewService.preview() — when request.libraryId() != null, resolve chunking/cleaning/normalization exclusively via LibraryConfigResolver; ignore request chunkSize/chunkOverlap/etc. except sampleText.
    REFACTOR: Remove dead toProperties() clamp path for libraryId-present requests.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — LibraryChunkPipelineParityTest</name>
  <files>knowbase-service/src/test/java/com/knowbase/vector/chunk/LibraryChunkPipelineParityTest.java</files>
  <read_first>
    - knowbase-service/src/test/java/com/knowbase/vector/service/ChunkPreviewServiceTest.java — sample text + expected counts
    - knowbase-service/src/main/java/com/knowbase/vector/chunk/LibraryChunkPipeline.java — from Plan 04-01
    - knowbase-service/src/main/java/com/knowbase/library/service/LibraryConfigResolver.java — chunkingFor signature
  </read_first>
  <behavior>
    - Test previewMatchesIndexPathForWeeklyReportSample: chunk() then chunkIndexedText(processedText) → equal chunks, rawTotalChunks, filteredOutCount
    - Test clientOverrideIgnoredWhenLibraryIdSet: ChunkPreviewService with libraryId + wrong chunkSize still returns weekly-report expected total=3
  </behavior>
  <action>
    Create test class with 杜鹏飞 fixture copied from ChunkPreviewServiceTest (single source — do not alter expected counts).
    Wire LibraryChunkPipeline with mocked LibraryConfigResolver returning ChunkingProperties(strategy=PARAGRAPH_FIRST, chunkSize=500, overlap=120, minParagraphLength=30, minChunkSize=80, maxChunkSize=1200).
    Mock normalization/cleaning as pass-through (identity) to isolate chunk+filter parity.
    Run test — MUST fail if ChunkPreviewService still honors client chunkSize override when libraryId present.
  </action>
  <acceptance_criteria>
    - Test file compiles and fails before ChunkPreviewService libraryId fix (RED)
    - Test names document PARITY-04 intent
  </acceptance_criteria>
  <verify>
    <automated>cd knowbase-service && mvn -q -Dtest=LibraryChunkPipelineParityTest test</automated>
  </verify>
  <done>Failing parity test committed as regression anchor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — libraryId-resolved preview + pass parity tests</name>
  <files>knowbase-service/src/main/java/com/knowbase/vector/service/ChunkPreviewService.java, knowbase-service/src/main/java/com/knowbase/vector/dto/ChunkPreviewRequest.java</files>
  <action>
    Inject LibraryConfigResolver into ChunkPreviewService (if not already via pipeline).

    In preview(): when request.libraryId() != null, call libraryChunkPipeline.chunk(libraryId, request.sampleText()) using resolver-backed config only — do NOT read chunkSize/chunkOverlap/minChunkSize/maxChunkSize/minParagraphLength/chunkingStrategy from request body.

    When libraryId is null (pre-create wizard): retain request-body chunk params via pipeline or existing toProperties path.

    Optional: add Jakarta validation note on ChunkPreviewRequest — chunk sizing fields optional when libraryId present (do not break API; document in JavaDoc only).

    Run LibraryChunkPipelineParityTest + ChunkPreviewServiceTest — both MUST pass (GREEN).
  </action>
  <acceptance_criteria>
    - LibraryChunkPipelineParityTest fully green
    - ChunkPreviewServiceTest still passes
    - chunk + chunkIndexedText parity proven on same processed text
  </acceptance_criteria>
  <verify>
    <automated>cd knowbase-service && mvn -q -Dtest=LibraryChunkPipelineParityTest,ChunkPreviewServiceTest test</automated>
  </verify>
  <done>PARITY-04 automated; client override cannot skew preview when libraryId set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/v1/index/chunk-preview | May send mismatched chunk params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | ChunkPreviewRequest chunkSize override | mitigate | Ignore sizing fields when libraryId != null; resolver is authority |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
- `mvn -q -Dtest=LibraryChunkPipelineParityTest,ChunkPreviewServiceTest test` green
</verification>

<success_criteria>
- Weekly-report sample: preview path chunk count equals simulated index path chunk count in automated test
</success_criteria>

<output>
Create `.planning/phases/04-preview-index-parity/04-02-SUMMARY.md` when done
</output>

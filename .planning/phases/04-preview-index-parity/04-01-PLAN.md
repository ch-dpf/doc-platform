---
phase: 04-preview-index-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knowbase-service/src/main/java/com/knowbase/vector/chunk/LibraryChunkPipeline.java
  - knowbase-service/src/main/java/com/knowbase/vector/chunk/ChunkPipelineResult.java
  - knowbase-service/src/main/java/com/knowbase/vector/service/IndexingService.java
  - knowbase-service/src/main/java/com/knowbase/vector/service/ChunkPreviewService.java
autonomous: true
requirements:
  - PARITY-01
  - PARITY-02
must_haves:
  truths:
    - "IndexingService and ChunkPreviewService produce identical chunk lists for the same input text and library config"
    - "IndexingService does not apply cleaning twice on parsed.txt (eliminates preview vs index boundary drift)"
    - "Both paths apply IndexingChunkFilter.removeHeaderOnlyChunks after ChunkingService.chunk"
  artifacts:
    - path: "knowbase-service/src/main/java/com/knowbase/vector/chunk/LibraryChunkPipeline.java"
      provides: "Shared normalize→clean→chunk→filter pipeline"
      exports: ["chunk", "chunkIndexedText"]
    - path: "knowbase-service/src/main/java/com/knowbase/vector/chunk/ChunkPipelineResult.java"
      provides: "rawTotalChunks, filteredOutCount, chunks"
  key_links:
    - from: "knowbase-service/src/main/java/com/knowbase/vector/service/IndexingService.java"
      to: "LibraryChunkPipeline.java"
      via: "chunkIndexedText on already-cleaned parsed.txt"
      pattern: "LibraryChunkPipeline|chunkIndexedText"
    - from: "knowbase-service/src/main/java/com/knowbase/vector/service/ChunkPreviewService.java"
      to: "LibraryChunkPipeline.java"
      via: "full pipeline on preview sample text"
      pattern: "LibraryChunkPipeline|chunk"
---

<objective>
Extract a shared `LibraryChunkPipeline` used by both `ChunkPreviewService` and `IndexingService`, and remove duplicate cleaning in the index path (PARITY-01, PARITY-02).

Purpose: Closes the core backend divergence documented in INGEST-PIPELINE.md §4.4 / 附录 A.2 — preview re-normalizes/cleans request text while index re-cleans already-cleaned `parsed.txt`, producing different chunk boundaries.
Output: `LibraryChunkPipeline` + refactored services; existing `ChunkPreviewServiceTest` still passes.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/docs/INGEST-PIPELINE.md
@.planning/docs/FILE-TYPE-PROCESSING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define LibraryChunkPipeline contract</name>
  <files>knowbase-service/src/main/java/com/knowbase/vector/chunk/LibraryChunkPipeline.java, knowbase-service/src/main/java/com/knowbase/vector/chunk/ChunkPipelineResult.java</files>
  <read_first>
    - knowbase-service/src/main/java/com/knowbase/vector/service/ChunkPreviewService.java — current normalize→clean→chunk→filter sequence
    - knowbase-service/src/main/java/com/knowbase/vector/service/IndexingService.java — index() cleaning + chunking + filter
    - knowbase-service/src/main/java/com/knowbase/ingest/service/DocumentPipelineService.java — parsed.txt written AFTER normalize+clean (lines 97–102)
    - knowbase-service/src/main/java/com/knowbase/vector/chunk/IndexingChunkFilter.java — removeHeaderOnlyChunks
    - knowbase-service/src/main/java/com/knowbase/library/service/LibraryConfigResolver.java — normalizationFor, cleaningFor, chunkingFor
    - knowbase-service/src/main/java/com/knowbase/library/config/VectorLibraryConfig.java — textNormalizationEnabled gate
  </read_first>
  <action>
    Create `ChunkPipelineResult` record with fields: `List&lt;String&gt; chunks`, `int rawTotalChunks`, `int filteredOutCount`, `String processedText` (final text after optional normalize+clean).

    Create `@Service LibraryChunkPipeline` injecting `ChunkingService`, `ParsedTextNormalizer`, `DocumentCleaningService`, `LibraryConfigResolver`, and global `ChunkingProperties` (for semanticSimilarityThreshold default).

    Expose two entry points:
    1. `chunk(UUID libraryId, String rawText)` — full pipeline: if `libraryConfigResolver.config(libraryId).isTextNormalizationEnabled()` then normalize with `normalizationFor(libraryId)`; apply `cleaningFor(libraryId)`; chunk with `chunkingFor(libraryId)`; filter with `IndexingChunkFilter.removeHeaderOnlyChunks`; return `ChunkPipelineResult`.
    2. `chunkIndexedText(UUID libraryId, String parsedText)` — index path ONLY: skip normalize+clean (parsedText is already pipeline output from DocumentPipelineService); chunk with `chunkingFor(libraryId)`; filter; return `ChunkPipelineResult`.

    Do NOT add ingest-profile or override-chunk parameters — library resolver is the single source per PARITY-01 and INGEST-PIPELINE D-14 target state.
  </action>
  <acceptance_criteria>
    - ChunkPipelineResult exposes rawTotalChunks and filteredOutCount computed before filter
    - chunk() mirrors ChunkPreviewService's prior behavior for normalize+clean order
    - chunkIndexedText() performs zero cleaning/normalization calls
    - Both methods use IndexingChunkFilter.removeHeaderOnlyChunks
  </acceptance_criteria>
  <verify>
    <automated>cd knowbase-service && mvn -q -Dtest=ChunkPreviewServiceTest test</automated>
  </verify>
  <done>Shared pipeline class exists with distinct full vs index-only entry points.</done>
</task>

<task type="auto">
  <name>Task 2: Wire IndexingService and ChunkPreviewService to shared pipeline</name>
  <files>knowbase-service/src/main/java/com/knowbase/vector/service/IndexingService.java, knowbase-service/src/main/java/com/knowbase/vector/service/ChunkPreviewService.java</files>
  <read_first>
    - knowbase-service/src/test/java/com/knowbase/vector/service/ChunkPreviewServiceTest.java — 杜鹏飞 fixture expectations (raw=4, filtered=1, total=3)
    - knowbase-service/src/main/java/com/knowbase/vector/dto/ChunkPreviewResponse.java — response shape
  </read_first>
  <action>
    Refactor `IndexingService.index()`: after `textFetcher.fetch(...)`, call `libraryChunkPipeline.chunkIndexedText(libraryId, text)` instead of manual `documentCleaningService.apply` + `chunkingService.chunk` + filter. Remove now-unused direct cleaning call on index path (PARITY-02).

    Refactor `ChunkPreviewService.preview()`: delegate to `libraryChunkPipeline.chunk(libraryId, request.sampleText())` when resolving from library config (keep request-body param path temporarily for backward compat until Plan 04-02 — if libraryId null, build ChunkingProperties from request as today but still route through pipeline's chunk-only helper or inline equivalent).

    Map `ChunkPipelineResult` to `ChunkPreviewResponse` preserving `totalChunks`, `rawTotalChunks`, `filteredOutCount`, and truncated chunk items.

    Preserve existing clamp logic for request-supplied chunk sizes only when libraryId is absent (wizard pre-create); do not change API contract.
  </action>
  <acceptance_criteria>
    - IndexingService no longer calls documentCleaningService.apply inside index()
    - ChunkPreviewService delegates chunk+filter to LibraryChunkPipeline
    - ChunkPreviewServiceTest.previewUsesIndexingChunkFilterAndLibraryChunkParams passes unchanged expectations
  </acceptance_criteria>
  <verify>
    <automated>cd knowbase-service && mvn -q -Dtest=ChunkPreviewServiceTest,IndexingChunkFilterTest test</automated>
  </verify>
  <done>Both services share one chunk+filter implementation; double-clean index bug eliminated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chunk-preview API → LibraryChunkPipeline | Untrusted client may send arbitrary sampleText and sizing fields |
| IndexingService → parsed.txt | Trusted internal storage; must not re-mutate beyond chunk+filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | ChunkPreviewRequest body params | mitigate | Plan 04-02 resolves sizing from libraryId via LibraryConfigResolver; ignore client overrides when libraryId present |
| T-04-02 | Denial of Service | Large sampleText in preview | accept | Existing request validation + truncate in response; same as today |
| T-04-03 | Tampering | parsed.txt content | accept | Internal storage path; out of scope for parity phase |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan |
</threat_model>

<verification>
- `mvn -q -Dtest=ChunkPreviewServiceTest,IndexingChunkFilterTest test` passes in knowbase-service
- Grep confirms IndexingService.index no longer calls documentCleaningService.apply
</verification>

<success_criteria>
- Same library config + same plain text yields identical chunk count and content from preview and index paths
- Du鹏飞 weekly-report fixture test still passes (raw=4, filtered=1, total=3 at chunkSize=500)
</success_criteria>

<output>
Create `.planning/phases/04-preview-index-parity/04-01-SUMMARY.md` when done
</output>

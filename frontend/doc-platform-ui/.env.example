# 开发（npm run dev）：一般无需配置，使用 Vite --host + /api 代理即可用 IP 访问
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080

# 生产构建（npm run build）部署到静态服务器时，指向真实后端：
# VITE_API_BASE=http://192.168.1.10:8080
# VITE_BACKEND_URL=http://192.168.1.10:8080
